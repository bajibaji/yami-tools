# AI 助手 × Claude Code 能力差距与路线图

> 目的：把 Open Yami 内置 AI 助手做到「不弱于 Claude Code」的生产力水平。
> 本文件是唯一对照基准（SSOT），每轮迭代更新「现状 / 差距 / 下一步」三栏。
> 更新时间：2026-09-14（第 12 轮：落地 2026-09-14 审计报告 `audit/REPORT.md` 的 P0/P1 修复 + 修正本文档自身的 6 处过期）。
> 版本水位：本条对应 `v1.7.3`（`probe-core.js:5` 的 `PROBE_VERSION`）。第 11 轮之后代码还走了这些（本文档此前完全没记）：
> 在场感知（引擎选中态/右键/焦点/鼠标停留四路信号）、审批续跑改走 `/approve/stream` 事件流、宿主崩溃兜底（`host-crash.log` 且不退出）、
> `ui_steps` 进审批门控、`/session/rewind`（从某一轮重来）、`/compact`（手动压缩）、授权新增「工具::*」档。

---

## 一、Claude Code 的生产力要素拆解（12 项）

| # | 能力 | 为什么它是生产力关键 |
|---|---|---|
| 1 | **流式输出** | 长任务里用户能看到「正在想什么、调用了什么工具」，而不是干等一个转圈 |
| 2 | **工具调用可视化** | 每次读文件/改文件/跑命令实时上屏，可中断、可追溯 |
| 3 | **会话持久化与恢复** | 关掉窗口、切机器、第二天继续，历史不丢 |
| 4 | **上下文预算管理** | 长会话自动压缩/摘要，不会因为爆上下文突然失忆 |
| 5 | **精确文件编辑** | 按行号读、按片段替换/插入，弱模型也能改对，不必整文件重写 |
| 6 | **检索能力（grep/glob）** | 先搜再改，而不是把大文件整份塞进上下文 |
| 7 | **待办清单（Todo）** | 多步任务有进度骨架，模型不会做一半跑偏 |
| 8 | **验证闭环** | 改完自动编译/校验，报错自动回读并修复 |
| 9 | **子代理委派** | 把「搜一遍全工程」这类脏活丢给子代理，主上下文保持干净 |
| 10 | **权限分级** | 只读自动、写入确认、危险操作强确认；模式只有 `confirm` / `auto` 两档，且 **`auto` 只豁免编辑器动作类，工程写盘任何模式都要确认**（见 `ai-host.js:1895`） |
| 11 | **回滚与审计** | 每次修改可回退，有变更清单和哈希证据 |
| 12 | **计划模式** | 大改动先出方案再执行，避免瞎改（**本工程裁决：不做**，用待办清单替代） |

---

## 二、现状对照（Open Yami AI 助手）

| # | 能力 | 现状 | 差距等级 |
|---|---|---|---|
| 1 | 流式输出 | ✅ 已落地（第 1 轮）。宿主 `/chat/stream`（SSE）实时推送 `start/status/delta/tool/notice/plan/steer/system/result/error`（`ai-host.js` 的 `startStream`）；前端逐字上屏；服务端不支持流式时自动降级为整段 | ✅ 已达标 |
| 2 | 工具调用可视化 | ✅ 已落地（第 1 轮）。每次工具调用推 `tool:start/done/fail/approval`，前端渲染成中文名的过程条（执行：读取脚本 · Assets/xxx.ts） | ✅ 已达标 |
| 3 | 会话持久化与恢复 | ✅ 已落地（第 1 轮）。会话落盘 `%APPDATA%\DanJuanDevSuite\sessions\<id>.json`；`GET /sessions` 列表、`GET /session/load` 恢复、`POST /session/delete` 删除；关窗口/重启编辑器/隔天回来都能续聊 | ✅ 已达标 |
| 4 | 上下文预算管理 | ✅ 已落地，**数字以代码为准（第 12 轮更正）**：单条工具结果上限 **24000 字符**（`ai-host.js:63-64`），常态修剪阈值 **8192 字符**（`context-meter.js:38`）；
窗口 **1M token**、占用达 **80%** 触发两级压缩（先确定性修剪，再模型摘要折叠成检查点），保留最近约 **16%** 窗口；刻度文案由宿主算好（`ai-host.js` `contextStatus`）；
**第 12 轮新增**：刻度可点开看「折叠了多少条 + 摘要正文」，并可**手动**「立即压缩」（`/compact`，复用同一条压缩路径）。旧版写的「32k / 240k / 最近 16 条」全部过期 | ✅ 已达标 |
| 5 | 精确文件编辑 | ✅ 已落地（第 2 轮）。`edit_script` 做**片段替换**：唯一性校验（多处命中直接拒绝并给出行号）、`dryRun` 预览差异、`expectedSha256` 冲突保护、写入后自动编译门禁、编译失败自动回滚。脚本不再必须整份重写 | ✅ 已达标 |
| 6 | 检索能力 | ✅ 已落地（第 2 轮）。`search_project` 内容检索：范围 script/data/event/all、正则或纯文本（非法正则自动退化）、命中带文件+行号+匹配位置、可选上下文行、结果上限与截断提示 | ✅ 已达标 |
| 7 | 待办清单 | ✅ 已落地（第 9 轮）。`todo_write` 让模型多步任务开工先列白话清单（3~6 步），每完成一步更新状态；前端有**任务计划卡片**（原地刷新，不刷屏），过程条实时显示「计划：进度 2/5，正在做：改脚本」；**禁止把已完成改回未完成**（进度倒退会误导用户）；改动小结里也带当前计划与进度 | ✅ 已达标 |
| 8 | 验证闭环 | ✅ 已强化（第 2~3 轮）。①**编译门禁修掉"静默失效"**：工程 tsconfig 出现配置级错误（TS5xxx）时 tsc 会只报配置错误、跳过代码检查，现在自动带 `--rootDir .` 重跑进入真实检查；②**编译失败自动修复循环**：写入没过编译 → 工具已自动回滚 → 宿主把 tsc 原始报错（含「文件:行号」）作为修复指令喂回模型，要求用 `edit_script` 做最小片段替换继续修；修满 `YAMI_AI_REPAIR_LIMIT`（默认 2）次仍不过则如实返回 `status='compile-failed'` 并说明工程已回滚，绝不假装成功 | ✅ 已达标 |
| 9 | 子代理委派 | ❌ 无 | P2 |
| 10 | 权限分级 | ✅ 已落地（第 4、8 轮）。①风险分级**实际只有两档在用**（medium / high；`server.js` 里无 `risk:'low'` 声明）：写盘类为 medium，审批卡片显示**真实差异**（逐行着色 + `+N/-M` 统计）；②`delete_resource` 为 high——预览给出影响摘要并发放**一次性确认令牌**，正式执行不带令牌一律拒绝，令牌用过即失效；③**会话内批量授权**（第 8 轮）：批准时勾选「本次任务内该文件不再逐条确认」，之后只有这个文件免打扰，其余文件照旧确认；**删除类永不授权**；授权可用 `/grants` 撤销，也可在【撤销】面板里一键取消；授权随会话落盘，重开窗口仍生效 | ✅ 已达标 |
| 11 | 回滚与审计 | ✅ 已落地（第 4~5 轮）。①每次写盘前自动备份，且**旁挂元数据**（改了哪个文件、由哪个工具改、时间、哈希）；②`list_backups` 列出可回退版本，`restore_backup` 一键回退（不传 backup 即回到**最早一次**＝AI 动手之前）；③回退前先把当前内容另存，所以**回退本身也能再撤回**；④`Data/manifest.json` 这类引擎派生的索引禁止回退；⑤前端工具栏【撤销】只列**本对话里 AI 改过的文件**，一点即回退并显示差异统计 | ✅ 已达标 |
| 12 | 计划模式 | ⛔ **不做**（2026-09-11 用户裁决：不需要该功能）。待办清单已提供「先列步骤再执行」的骨架，再叠一层独立计划模式属于重复能力 | 不做 |
| 13 | 打转保护（额外项） | ✅ 已落地（第 1 轮） | ✅ 已达标 |
| 14 | **运行时诊断入上下文（本工程特色）** | ✅ 已落地（第 2 轮）。`diagnose_runtime` 把插件采集的**报错黑匣子**压成 AI 可直接下结论的清单：报错按指纹聚合、每条带可疑文件名+行号+就地源码+白话归因+发生次数，另有卡住/幽灵事件、最耗时更新器与渲染器、内存与资源缓存。体积约十几 KB（对照 `/report` 数百 KB 且含 300 帧时间线） | ✅ 已达标 |
| 15 | **领域工作流内建（本工程特色）** | ✅ 已落地（第 2 轮）。系统提示词写明本工程约定与推荐工作流：先 `search_project` 定位再读单文件；改脚本优先 `edit_script` 片段替换而非整份重写；资源/事件/数据表/GUID 的领域常识；「用户说游戏里报错」→ 先 `diagnose_runtime` 拿行号 → 改 → `compile_check` → 复现验证 | ✅ 已达标 |
| 16 | **试玩冒烟测试（本工程特色，第 6 轮）** | ✅ 已落地。`playtest_smoke` 按脚本驱动试玩（`"down,down,ok"` 或对象数组，支持按住时长/等待），跑完**自动对比前后诊断**，直接回答「新报错 / 变频繁的报错 / 新卡住的事件 / 性能是否恶化」，并给出 verdict 与定位提示。未在试玩中时明确要求用户先启动试玩，绝不假装跑过 | ✅ 已达标 |
| 17 | **改动小结（本工程特色，第 7 轮）** | ✅ 已落地。`project_changelog` 用**内容哈希对比基线**算真实变更（写了又改回去、写失败回滚都不计），并合并编译结论、试玩结论与下一步建议；任务收尾自动挂到返回值上，前端渲染成小结卡片；打开面板即自动建立基线，只报本次对话增量 | ✅ 已达标 |

### 已达标/领先项（保持）
- **写盘安全**：所有写操作先预览、带 `expectedSha256` 冲突检测、原子替换、自动备份 —— 比「直接改文件」更稳。
- **真实编译门禁**：脚本写入强制跑引擎自带 tsc，编译失败自动回滚。
- **三层控制**：工程文件（MCP）/ 编辑器动作（5967）/ 试玩输入（5966）全覆盖。
- **零依赖**：无 npm 依赖，断网可用。

---

## 三、第 1 轮（已完成，可验证）

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 流式输出 SSE | `ai-host.js` `requestModelStream` + `/chat/stream` | `tests/test-ai-session.cjs` 第 1 组：delta 可拼成完整回复、result 状态 done |
| 工具调用可视化 | 宿主推 `tool` 事件 + `ai-agent.js` 过程条 | 同上第 5 组：工具 start/done 事件、且发生在 result 之前 |
| 会话持久化 | 会话落盘 + `/sessions`、`/session/load`、`/session/delete` | 同上第 2、3 组：列表标题、文件落盘、宿主重启后历史可恢复 |
| 上下文管理 | 工具结果裁剪 + 历史压缩（模型摘要/静态折叠） | 同上第 4 组：超预算后折叠为摘要、最近消息保留、占用回落 |
| 打转保护 | `continueSession` 重复调用检测 | 同上第 6 组：连续重复即止损，模型调用 3 次而非撞满 12 步 |
| 前端历史面板 | 工具栏【历史】+ 上下文刻度 + 新对话不再删除历史 | 构建门禁 + 人工走查 |

> 本轮顺手修掉的真 bug：模型重复调用同一工具时会一路空转到 12 步上限（测试第 6 组抓到并已修复）。

## 四、第 2 轮（已完成，可验证）：按本工程需求做特色化

Claude Code 的通用能力不是目标本身；这一轮只做「Open Yami 工程里真正卡效率」的事。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| `search_project` 内容检索 | `runtime/yami-mcp/server.js` | `tests/test-mcp-ai-tools.cjs` 第 1 组：范围过滤、行号与匹配位置、上下文行、正则、非法正则退化、无命中提示 |
| `edit_script` 精确片段替换 | 同上 | 同第 2 组：不存在拒绝、多处命中拒绝并给行号、dryRun 不写盘、冲突保护、写入后编译门禁、**编译失败自动回滚**、越界路径拒绝 |
| `/diagnose` 诊断端点（插件侧） | `probe-core.js` `buildDiagnosis()` | 同第 3 组：无试玩时安全降级；有试玩时校验结构且体积 < 60KB |
| `diagnose_runtime` 工具 | `runtime/yami-mcp/modules/runtime-bridge.js` + server 注册 | 同上（旧版插件缺少 `/diagnose` 时自动退回精简报告） |
| 编译门禁修复 | `runCompileCheck()` | 实测：工程 tsconfig 配置级错误曾导致"只报配置错误、跳过代码检查"；现在自动 `--rootDir .` 重跑，语法错误必被抓到 |
| 领域工作流提示词 | `ai-host.js` `SYSTEM_PROMPT` | 先搜→再读→片段改→编译验证→试玩复现；含资源/事件/数据表/GUID 约定 |

> 本轮踩坑记录（写成注释留在代码里）：把语法错误注入到 `/* @plugin ... */` 注释块**内部**时，编译器不会报错——那不是"门禁失效"，是测试构造错了。测试已改为往真代码锚点注入。

## 五、第 3 轮（已完成，可验证）：编译失败自动修复闭环

「改完跑编译、不过就自己修」是这类工具最值钱的一环；这一轮把它做进宿主主循环。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 编译失败识别 | `ai-host.js` `compileFailureOf()` | 识别 `ok:false + compile.ok:false`，带出错误条数、首条 `文件:行号`、tsc 原文、是否已回滚 |
| 修复指令生成 | `repairMessage()` | 要求：先定位再改、**只做最小片段替换**、改完必须确认编译、同一报错两次不过就停下讲原因 |
| 主循环接线 | `continueSession()` | 同一轮内检测到编译失败 → 推 `notice` 事件 → 注入修复指令 → 继续（不再直接收工） |
| 用户确认路径同样生效 | `/approve` 分支 | 手动确认的写入编译失败也走同一套自动重修，不是只有自动模式才有 |
| 上限与如实失败 | `MAX_REPAIR_ATTEMPTS`（默认 2） | 修满仍不过 → `status='compile-failed'` + 最后一次报错 + 说明工程已回滚 |
| 修复预算重置 | `/chat` 入口 | 每条新需求重置 `session.repairs`，上一轮的失败不会拖累下一轮 |

测试：`tests/test-ai-repair.cjs` —— 用"先写坏、收到报错后改对"的假模型验证整条回路，并验证修不好时如实失败、工程不留半成品。

## 六、第 4 轮（已完成，可验证）：改动看得见 + 危险操作拦得住

游戏工程是"改错一次就崩档"的场景，所以审批环节必须让人**看见具体改了什么**、让不可撤销的动作**必须二次确认**。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 统一差异工具 | `runtime/yami-mcp/modules/diff.js` | `tests/test-mcp-approval-diff.cjs` 第 1 组：增删统计、定位块、上下文保留、无变化表述、超大差异截断 |
| 写盘预览带真实差异 | `edit_script` / `patch_resource` 的 dryRun | 同第 2 组：`diff` 含 `@@` 与 `+` 行、风险级别、`diffStat` |
| 前端差异渲染 | `ai-agent.js` `renderDiff()` + 审批卡片容器 | 逐行着色（新增绿/删除红/定位蓝 + `+N/-M`）；接线断言在 `tests/test-ai-agent.cjs` |
| 删除二次确认 | `delete_resource` + 一次性令牌 | 同第 3 组：高危标记、影响摘要、无令牌被拦（`ok:false` + `confirmRequired`）、伪造令牌被拒、令牌用过即失效 |
| 热更新清单补全 | `probe-core.js` `updateFiles` 增补 `diff.js` | `tests/test-autoupdate.mjs` 新增断言：**声明的清单必须覆盖 modules 下全部模块**（漏一个，别人热更新后 MCP 直接崩） |
| 热更新容错 | `performAutoUpdate` | 个别文件远端暂缺（推送落地前的窗口）不再让整次更新失败：跳过并记入 `missingFiles` 如实报告；但 `probe-core.js`/`manifest.json`/`server.js` 缺失时仍然中止 |

> 坑位记录：`edit_script` 原来的预览里已有一个 `diff` 字段（片段前后对照），会把新生成的统一差异**覆盖掉**，宿主只拿到 15 个字符。已改名 `fragmentDiff` 并写进注释。

## 七、第 5 轮（已完成，可验证）：改错能一键回退

让用户敢让 AI 改工程的前提不是"写得准"，而是"**改错能退回去**"。这一轮把备份从"存在但看不见"变成"看得见、点得到、退得回"。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 备份旁挂元数据 | `modules/file-ops.js`：每个 `.bak` 配一份同名 `.json`（路径/工具/时间/哈希/体积） | `tests/test-mcp-approval-diff.cjs` 第 4 组：备份内容 = 改动前内容、带时间与来源工具 |
| `list_backups` 工具 | `server.js` | 同上：列出可回退版本、时间、来源工具、体积 |
| `restore_backup` 工具 | `server.js` | 同上：默认退回最早一次、dryRun 预览差异、安全备份、**撤销的撤销**、manifest 禁止回退、无备份如实说明 |
| 前端【撤销】面板 | `ai-agent.js` `renderUndoList()` + `ai-undo` 样式 | 只列本对话改过的文件，一点回退并显示 `+N/-M`；接线断言在 `tests/test-ai-agent.cjs` |
| 宿主接口 | `/backups`、`/backup-undo` | `tests/test-ai-repair.cjs` 第 3 组：只列本对话文件、回退成功、回退后内容回到最初 |
| 审批门禁修正 | `ai-host.js` `FILE_MUTATIONS` | `edit_script` **此前不在写盘审批集合里**——走这个工具改脚本不会弹审批卡。已纳入，并新增第 5 组断言守着（确认模式下必须停在审批、审批前工程不得改动、取消后不得改动） |

> 坑位记录（两处真缺陷，都由测试抓到）：
> 1. `writeAtomic` 的备份路径从绝对改成相对后，`restoreBackup` 仍按绝对路径处理，导致**编译失败自动回滚静默失效**；
> 2. `edit_script` 漏在 `FILE_MUTATIONS` 之外，写盘绕过审批。

## 八、第 6 轮（已完成，可验证）：AI 能自己跑一遍试玩并报坏

通用编码助手永远做不到的一件事：**把游戏跑起来、走两步、再把运行时黑匣子的变化读回来**。这一轮把"改完到底还能不能玩"变成一条可自动执行的验证。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 动作脚本规整 | `modules/playtest.js` `normalizeSequence()` | `tests/test-playtest-smoke.cjs` 第 1 组：字符串简写、别名（ok→Enter）、按住时长按需下发、非法按键拒绝、步数上限、空脚本 |
| 诊断对比判定 | 同文件 `diffDiagnosis()` / `describeVerdict()` | 同第 2 组：无变化判 ok、新报错/变频繁/新卡住/性能恶化各自识别、结论话术可直接给用户 |
| `playtest_smoke` 工具 | `runtime-bridge.playtestSmoke()` + server 注册 | 同第 3、4 组：无试玩时安全降级并要求先启动试玩；空脚本拒绝；**完整成功路径**用模拟 5966 桥验证按键真的下发、前后各取一次诊断、识别新报错（带文件行号）与新卡住事件、给出前后性能快照 |
| 端口可覆盖 | `YAMI_RUNTIME_BRIDGE_PORT` | 让测试与真机共用同一套代码路径，不写死 5966 |
| 热更新清单 | `probe-core.js` 增补 `modules/playtest.js` | `tests/test-autoupdate.mjs` 的「声明清单覆盖全部模块」断言守着 |

## 九、第 7 轮（已完成，可验证）：改完说清"到底动了什么"

前六轮解决了改得准、看得见、退得回、跑得动；这一轮把收尾补上——用户不必自己比对就知道这次改了什么、验证到哪一步、下一步该做什么。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 快照对比 | `modules/changelog.js` `snapshotProject()` / `diffSnapshot()` | `tests/test-changelog.cjs` 第 1 组：修改/新建/删除识别、哈希不变即无变更 |
| 小结合成 | 同文件 `buildChangelog()` / `describeChangelog()` | 同组：统计正确、来源工具与编译结论标注、话术含编译与试玩结论、全回滚时如实说明 |
| `project_changelog` 工具 | `server.js`（含 `reset` 基线语义） | 第 2 组：建立/重置基线、识别真实变更、**写了又改回去不计入变更**、新建文件归类 |
| 写入记录 | `server.js` `rememberWrite()` | 小结里每个文件都带来源工具与编译结论；试玩后带 verdict |
| 收尾自动小结 | `ai-host.js` `attachChangelog()` | `tests/test-ai-repair.cjs`：净变更为 0（被回滚）时如实计 0、不伪造小结 |
| 前端小结卡片 | `ai-agent.js` `renderChangelog()` + `ai-changelog` 样式；打开面板自动建基线（`/changelog-baseline`） | 构建门禁 + 接线断言 |

## 十、第 8 轮（已完成，可验证）：把"逐条确认"从阻力变成可控授权

一件任务里同一个文件常要连着改好几次，每条都弹审批会把人磨烦；而"逐条确认"一旦变成走过场，反而更危险。
做法不是取消确认，而是让用户**显式授权**、并且随时能收回。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 授权判定 | `ai-host.js` `isGranted()` / `addGrant()` | `tests/test-ai-repair.cjs` 第 5 组：首次仍需确认 → 勾选后批准 → 同文件同工具不再打断 → 撤销后恢复逐条确认 |
| 授权持久化 | 会话落盘带 `grants`，恢复会话时带回 | 同上（授权随会话保存，重开窗口仍生效） |
| 授权查询与撤销 | `POST /grants`（列出 / `revoke` 单个 / `clear` 全部） | 同上：`/grants` 列出带工具中文名与文件；撤销后重新逐条确认 |
| 删除类免疫 | `DELETE_TOOLS` 判定 | 同上末条：授权列表与方法都不接受 `delete_resource`（不可轻易撤销的动作必须逐条确认） |
| 前端授权体验 | 审批卡片勾选（删除类自动隐藏该选项，偏好记忆在 localStorage）；【撤销】面板内列出当前授权并可一键取消 | 构建门禁 + 接线断言 |
| 过程可见 | 免打扰执行时推 `notice`：「已授权：精确改脚本 · <文件>（本次任务内不再逐条确认，随时可撤销）」 | 同上 |

> 设计取舍：批量授权之所以现在才做，是因为**必须先有兜底**——写盘有备份与差异统计（第 4 轮）、收尾有改动小结（第 7 轮）、随时可一键撤销（第 5 轮）。没有这三样，批量授权等于把风险放大。

## 十一、第 9 轮（已完成，可验证）：多步任务有进度骨架

复合需求（"检查一遍并修好"、"给战斗加个新机制"）最怕两点：用户不知道 AI 做到哪了、模型做一半跑偏。待办清单同时解决这两件事。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 清单规整与校验 | `modules/todos.js`：`normalizeTodos()` / `summarizeTodos()` / `validateTransition()` / `renderTodos()` | `tests/test-todos.cjs` 第 1 组：字符串与对象数组、非法项拒绝、上限、统计话术、**进度不可倒退**、纯文本渲染 |
| `todo_write` 工具 | `server.js`（含 `clear`） | 第 2 组：创建 → 推进 → 进度话术 → 禁止倒退 → 全部完成提示可收尾 → 清空 |
| 与小结联动 | `project_changelog` 返回 `todos` / `todoSummary` | 第 3 组：小结带计划与进度、清空后不再带 |
| 过程可见 | 宿主推 `plan` 事件 + `notice`「计划：进度 N/M，正在做：…」 | `tests/test-ai-agent.cjs` 接线断言 |
| 计划卡片 | `ai-agent.js` `renderPlan()`（**同一张卡片原地刷新**）+ `ai-plan` 样式 | 构建门禁 + 接线断言 |

> 细节取舍：进度只允许前进不允许后退（把 `done` 改回 `pending` 会被工具拒绝）——否则用户看到进度倒退会以为出了故障；卡片原地刷新而不是每步新插一张，避免长任务刷屏。

## 十二、第 11 轮（已完成，可验证）：只读工具并发 + 一个隐藏缺陷

**动机**：模型经常在一条消息里请求多个只读工具（一起读几个文件/查几处代码），宿主原来是串行执行的。

**过程中抓到的真缺陷**：宿主自己维护了一份「只读工具清单」副本。副本与 MCP 注册表当前恰好一致（各 18 项），但**将来任何新增的只读工具都会被漏掉**，从而被当成独占调用——并发永远不生效，而看代码完全看不出来。实测证据：到达跨度 802ms（≈ 串行）。

**修法**：以 MCP 注册表的 `readOnlyHint` 为**真源**（工具自己声明是否只读），宿主在 `McpClient.start()` 时拉取一次填充 `declaredReadOnlyTools`；静态清单退化为兜底；未声明/未登记的工具一律按独占处理（宁可慢，也不误并发写操作）。

| 交付 | 落点 | 验证方式 |
|---|---|---|
| 只读并发执行 | `ai-host.js` `isExclusiveCall()` / `runReadOnly()` / 批次合并 | `tests/test-parallel-tools.cjs`：三个 400ms 只读调用**到达跨度 1ms、端到端 418ms**（修复前 802ms / 1222ms） |
| 结果顺序 | 批次内按请求顺序回填 tool 消息 | 同上：`["a","b","c"]` |
| 写盘独占 | 批次在写盘处断开 | 同上第 2 组：混合调用时写盘仍弹审批、之前的只读已完成 |
| 真源接线 | `declaredReadOnlyTools` + `readOnlyHint === true` | `tests/test-ai-agent.cjs` 接线断言 |

> 结论与取舍：修好之后并发确实生效，但要说清收益边界——本项目里 MCP 是同机子进程、只读工具通常几十毫秒返回，**端到端收益有限**；这条改动的真实价值是「消除了一份会腐化的清单副本」，而不是"大幅提速"。

## 十三、后续轮次候选（按价值排序）

> 第 15 轮更新：本节此前只有两条，已不能当路线图用。现行的差距与优先级以 **`audit/REPORT.md`（2026-09-14 审计，含 P0/P1/P2 与每条的最小修法）** 为准；第 12 轮（审计修复）见第十四节，第 13 轮（对话导出）见第十五节，第 14 轮（实测反馈修复）见第十六节，第 15 轮（屏上与上下文对齐）见第十七节 —— 下面这份候选清单本身不变。

- **真机 GUI 走查**（SSOT 与 HANDOFF 3.3 都列着，仍是唯一没做过的验收方式）
- **P2 · @ 引用工程文件**（G-3）：输入框打 `@` 从工程文件列表里选，仅作为文本插入，不改协议
- **P2 · 过程区折叠的「看全」**（G-7 剩余部分）：本轮已让折叠条报出工具名，进阶是 `Ctrl+O` 式全量转录视图
- **P2 · 子代理委派**：保持 P2，不计差距（HANDOFF 3.1/3.3 已声明）
- **明确不做**：附件/图片输入（G-4，属新能力）、hooks/自定义斜杠命令（G-9，与「计划模式不做」同构）

---

## 十四、第 12 轮（已完成，可验证）：审计修复（2026-09-14）

**动机**：2026-09-14 的独立审计（`audit/REPORT.md`：六人团队、66 个锚点逐条取证、19 条 findings 全关）指出交互逻辑上的 3 条 P0 与 4 条 P1。本轮把它们落地，并修掉本文档自身的 6 处过期。

| 项 | 修法 | 落点 | 验证方式 |
|---|---|---|---|
| **G-1** 消息编辑 / 重发 / 从某一轮重来 | 宿主新增 `POST /session/rewind`（把 `session.messages` 截断到该条用户消息之前并落盘，原文回填面板）；面板用户气泡加「重发 / 编辑」两颗按钮 | `ai-host.js`；`ai-agent.js` `attachUserActions` / `rewindTo` | `tests/test-ai-session.cjs` §9（真宿主：轮次号 / 原文回填 / 截断落盘 / 截断后仍可继续 / 越界如实报错）|
| **G-5** 审批续跑不吃停止 | `2290` 补第 6 参 `cancelToken`；被批准的写盘改走 `callToolWithCancel` | `ai-host.js` 审批分支 | `tests/test-ai-agent.cjs` 接线断言 |
| **G-10(a)(b)** | `decide` 的 finally 补 `flushQueue()`；等待卡一次收尾全部 | `ai-agent.js` | 同上（函数体判定 + `querySelectorAll` 断言）|
| **G-6** 审批粒度补中间档 | 授权键新增 `工具::*`（无 `path` 的写盘工具不再永远拿不到授权）；审批卡加第二档勾选；文案改成「本对话内…跨重启仍有效」 | `ai-host.js` `grantKeyOf`/`isGranted`/`addGrant`/`/grants`；`ai-agent.js` 审批卡 | 接线断言 + `tests/test-ai-repair.cjs` 授权用例 |
| **G-8** 上下文控制权 | 刻度可点开（折叠条数 + 摘要正文）；新增「立即压缩」走 `POST /compact`（`force` 跳过 80% 阈值，复用同一条压缩路径） | `ai-host.js` `compressContext(..., force)` / `/compact` / `contextStatus`；`ai-agent.js` `renderContextDetail` / `compactNow` | 接线断言 |
| **G-11** `ui_steps` 进门控 | 并入 `OTHER_MUTATIONS`：confirm 模式下 AI 改编辑器界面前必须先确认（auto 模式照旧） | `ai-host.js:43` | 接线断言 |
| **G-10(d)** v1.7.0 子视图隐藏规则失效 | 选择器从两个不存在的 id 改成真实的 `.yami-ai-compose` / `.yami-ai-devbar`；**并把测试从「断言 CSS 文本含选择器」改成「断言选择器指向的元素真的存在」** | `src/style.css`（经 `build.cjs` 注入 `hud-overlay.js`）；`tests/test-subviews-floating.cjs` | 该套件 21/21（新断言本身就能抓住这类回归）|
| **G-10(c)(e)** | 撤销/重做繁忙时给 toast 而不是静默返回；折叠控件补 `aria-expanded`、审批卡补 `aria-modal` / `aria-labelledby` | `ai-agent.js` | 接线断言 |
| **G-7** 折叠条可见性 | 过程区摘要附上本轮工具名（刷新时只读数组、不遍历 DOM） | `ai-agent.js` `refreshProcessMeta` | 接线断言 |
| **S-1~S-6** 本文档自身 | 版本水位、事件清单、上下文数字、权限档位、路线图、更新时间 6 处过期已更正 | 本文件 | 本轮评审逐条复核 |

> 口径说明（写在轮次里，免得下次又猜）：`/session/rewind` **只回退对话，不回退文件** —— 盘上的改动仍在，要回退请用【撤销】面板（`restore_backup`）。这是刻意的拆分：对话时间轴与文件状态是两个独立事实，混在一起回退会造出"我撤销了对话、文件却被改回去"的新惊吓。

---

## 十五、第 13 轮（已完成，可验证）：把对话导出成文档（2026-09-14）

**动机**：用户直接提的需求 —— "能不能导出对话？包括历史对话"。此前会话只以内部 JSON 落在 `~/DanJuanDevSuite/sessions/*.json`，人读不了、也拿不出去（历史子页只能一段段切回去看）。

| 项 | 做法 | 落点 | 验证方式 |
|---|---|---|---|
| 单段导出 | `POST /session/export {sessionId}` → `{filename, markdown}` | `ai-host.js` `sessionToMarkdown()` | `tests/test-ai-session.cjs` §10 |
| 全量导出 | `{all: true}`：逐段排版 + 顶部目录（空会话不入稿） | 同上 + `exportFileName()` | 同上 |
| 只读保证 | 不写会话文件、不动内存会话、渲染不碰模型（所以忙碌中也能导） | `ai-host.js` 导出分支 | 同上：单段 + 全量两次导出后会话文件**逐字节比对未变** |
| 稿件取舍 | system 提示词与工具结果不进正文（只记条数 / 只留「执行：」步骤行）；思考折叠进 `<details>`；宿主自动追加的 user 消息标成「系统提示（宿主自动追加，非用户发言）」 | `ai-host.js` `HOST_NOTE_PATTERNS` / `exportArgsHint()` | 同上：两条"不进正文" + 一条"不冒充用户发言"断言 |
| 面板入口 | 工具条【导出】、历史子页【导出全部】、历史每条【导出】 | `ai-agent.js` `exportConversation()` | `tests/test-ai-agent.cjs` 12 条接线断言 |
| 落盘 | **插件数据目录**（`%APPDATA%/DanJuanDevSuite/exports/`）+ 资源管理器定位；目录不可写才复制正文到剪贴板并如实说明 | `ai-host.js` `writeExport()` / `ai-agent.js` `revealAbsolute()` | 接线断言 + §10「绝不落进用户工程」 |

> 口径说明：导出的 Markdown 是**给人（和别的工具）读的稿子**，不是可回灌的备份格式 —— 会话的唯一权威仍是 `sessions/*.json`；`/session/export` 也因此不做任何写操作，不构成第二条会话写入路径。
---

## 十六、第 14 轮（已完成，可验证）：从一份真实会话里挖出的 8 个坑（2026-09-14）

**动机**：用户拿一段真实会话（"写个施放技能的事件测试"，2 轮 / 65 条消息 / 37 次工具调用）来问"这个 AI 助手不好用，MCP 好像也没介入，写入的位置也是错的"。逐条查证后的结论是：**MCP 全程真的在工作**（37 次调用 34 次成功，3 次失败全是它自己选了 F5 而按键通道不认功能键），真正的问题是下面 8 处 —— 其中两处是插件自己的设计缺陷。

| # | 问题（证据） | 修法 | 落点 | 验证方式 |
|---|---|---|---|---|
| **F1** | 工程根下的 `DANJUAN TOOLS/00-文档总索引（AI 阅读入口）.md`（专为 AI 写的十几万字知识库）**从未进过模型上下文** → 5 次检索 + 6 次读取全在盲猜 | 宿主动态探测该文档并跟环境快照走同一条注入通道（不存在就一个字不提） | `ai-host.js` `projectDocHint()` | `test-ai-session.cjs` §1 行为断言（断言模型实际收到的 system 正文含「工程文档入口」） |
| **F2** | 宿主连提示 6 次「你已经查了很多遍」，模型照旧往下查（换的是关键词，旧的打转保护只认完全相同的调用） | 预算硬闸：单工具 8 次 / 单轮 30 次，到点 `status: 'stuck'` 并报出本轮调用分布 | `ai-host.js` `TOOL_REPEAT_LIMIT` / `TURN_CALL_BUDGET` | `test-ai-session.cjs` §12（真链路：9 次模型调用内被拦下） |
| **F3** | 写盘后的编辑器热更新失败被 `try/catch {}` 静默吞掉 —— 后果是编辑器内存仍旧、**用户下次保存会覆盖这次改动** | 热更新结果留痕，失败时挂到工具结果上让模型转告用户 | `runtime/yami-mcp/server.js` `notifyEditorReload()` / `tools/call` 收尾 | 接线断言 |
| **F4** | 工具说明没写按键白名单 → 模型设计了 F5 触发的测试，到最后才发现跑不了，只能请用户手动按 | 白名单写进两个试玩工具的说明 | `send_player_input` / `playtest_smoke` schema | 接线断言（两处说明各钉一条） |
| **F5** | 上一轮把整个 `ui_steps` 并进 `OTHER_MUTATIONS`，连高亮演示都要确认 → 这轮 37 次调用里 `ui_steps` **一次没用** | 按步骤类型细分：`set`/`click` 要确认，`focus`/`goto`/`wait` 放行 | `ai-host.js` `uiStepsMutating()` | `test-ai-session.cjs` §11（focus 不弹确认卡 / set 仍要确认） |
| **F6** | 导出稿落进了用户工程根目录（那是他的 git 仓库，直接变脏文件） | 落点改为插件数据目录；宿主排版+落盘，面板只报落点并定位 | `ai-host.js` `writeExport()` / `ai-agent.js` `revealAbsolute()` | §10「落在宿主数据目录、绝不落进用户工程」 |
| **F7** | `project_changelog` 首次调用只建基线不报内容 → 模型自己发现后干脆不报清单，用户拿不到"改了哪些文件" | 首次调用也交"上次汇报之后的写入记录"；文件清单并入写入条目（标 `fromWrite`） | `runtime/yami-mcp/server.js` / `modules/changelog.js` | 接线断言 |
| **F8** | 有停留点（"停在「攻击力」=25"）时就只报停留点 → 用户鼠标选中的技能不进提示词，他明明选了技能模型还反问"先测哪个" | 停留点之外**永远带上"选中「xxx」"**（场景对象优先，其次资源树选中项），仍两句封顶 / 120 字上限 | `probe-core.js` `formatEditorContextSummary()` | `test-ai-agent.cjs` vm 实跑断言（含长度上限） |

> 口径说明（写在轮次里，免得下次又猜）：**"只报停留点"这条老规矩被本轮改掉了** —— 用户说"这个技能/这个角色"时，指的就是资源树里选中的那个；只报"他停在哪个控件"等于让他再解释一遍。现在的取舍是：停留点 + 选中的资源两句封顶，页面/场景这类他自己看得见的背景仍然不占那一行。
---

## 十七、第 15 轮（已完成，可验证）：屏上与上下文说同一件事（2026-09-14）

**动机**：用户重新进入 AI 助手页，屏上只有一句欢迎语，他发了新需求，模型却在思考里复述上一轮的结论（"上一轮已完成：F5 施展冲撞"）—— 也就是"刚进去的对话为什么会有上一次的记忆"。

| 项 | 做法 | 落点 | 验证方式 |
|---|---|---|---|
| 现象实锤 | 新消息落进了**老会话文件**（`session-mu0moxka.json` 的用户消息从 2 条涨到 4 条）—— 屏上说"新对话"，上下文里是旧对话 | 会话文件本身 | 直接读盘比对 |
| 修法 | 进页面若消息区已经聊过，就开一段**新会话**（换 sessionId，旧对话都在【历史】里）；正在跑的一轮不换会话，改成把上次那段回放到屏上对齐 | `ai-agent.js` `startNewSession()` / `messagesPristine()` / `restoreLastSession()`（忙时用） | `tests/test-ai-agent.cjs` G-12 契约断言 |

> 口径说明：**屏上和上下文必须说同一件事**。进页面这条路本轮最终定为"开新会话"（用户拍板：进去就是新的，旧的在【历史】里）；`新对话` 一直是真换 sessionId，`/clear` 也只清内容不删文件。唯一例外是**正在跑的一轮**：换会话会把它晾在后台，所以那种情况反过来把上下文回放到屏上。
>
> 计划卡片的规矩也一并写在这里：**折叠时只占一行，显示"当前在做什么"** —— 完整清单点开看。多步任务的进度骨架不该把对话挤走。
