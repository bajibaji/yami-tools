# 效率工具链审核与改进报告

> 审核对象：`D:\Documents\GitHub\yami-tools` 四个 Web 效率工具（角色编辑器 / 地图编辑器 / 挂机验证台 / 快速本地化）+ `D:\new-game\DANJUAN TOOLS` 配套 AI 基建（yami-mcp / 自动刷新方案 / yami-plugins-bulider / AI·reasonix）。
> 参照物：`D:\new-game\DANJUAN TOOLS` 四份架构文档（项目架构文档 / Yami引擎机制 / Yami引擎编写规则 / Yami经验笔记）、开源引擎源码仓库 `D:\Documents\GitHub\2`、实际安装引擎 `D:\Program Files\Open Yami RPG Editor`（2026-07-25 构建）。
> 审核方法：文档交叉核对 + 引擎安装目录与源码仓库实证 + 工具自检/回归实测 + 源码级审查。
> 日期：2026-08-15（待最终定稿）

## 0. 结论摘要（TL;DR）

1. **四个 Web 工具总体健康**：自检全过（node --check + self-check），安全机制（备份/回滚/外部修改保护）完善，是当前工作流里最成熟的一环。但有 3 个必须修的数据正确性问题：① 快速本地化「数据属性」sheet 的 Excel 导入在浏览器实际路径上**静默失败**（条目建了、资产文件没替换）；② 角色编辑器手动「重新扫描」会**清空全部未保存草稿**；③ 角色编辑器写回格式不仿生（LF+尾换行 vs 引擎 CRLF+无尾换行），每次保存全文件 diff。
2. **最痛的生产问题「外部改文件编辑器不刷新」已经解决了一半**：自动重载模块已在引擎源码仓库实现，但**未提交、未构建、未部署**——安装中的引擎仍是 7/25 旧构建，痛点依旧每天发生。且实现里有 **2 个必死 bug**：dev 模式 `File.route` 返回 URL 而非路径（按官方指南验证会"看起来没生效"）；`tickRoot` 每 2 秒重启 watcher 并清空防抖队列（实际延迟退化为 5-8 秒）。另外 `DANJUAN TOOLS` 里交付的两个 .js 补丁是旧架构死代码（依赖的 window 全局在新引擎里全部不存在）。
3. **yami-mcp 实际处于半离线状态**：`D:\new-game\.mcp.json` 不存在（README 声称"已注册"），AI 会话拿不到 MCP 工具；回归测试 10/11 过（唯一失败是硬编码统计数字漂移 51→53）。另有：**路径穿越漏洞**（`..` 可读写项目根之外的文件）、无法写 `Data/commands.json`/`plugins.json`（AI 生成的脚本无法注册）、**validate_project 引用完整性报 5584 条误报**（90%+ 假告警，能力≈失效）、write 不查全工程 GUID 冲突、无 RLE 防破坏保护、README「聚焦即刷新」前提不成立。
4. **文档体系整体漂移到旧架构**：`Yami引擎机制.md` §2/§10 的全部路径（`resources/app/Script/…`、`html/head.html`）与「window 全局单例」认知都对应旧引擎；当前安装引擎已是 vite 打包 + 真 ESM（`dist/index.html` + `dist/assets/index.js` 5.3MB 单包，window 业务全局全消失），源码在 `D:\Documents\GitHub\2\Project\Script/**/*.ts`。照旧文档打补丁会打错位置；SKILL.md 还藏有 onStart/onStartup 等会误导 AI 生成坏代码的事实错误。
5. **版本显示事故**：`tools/version.json` 把挂机验证台标成 `0.1.0 beta`（实际 v0.9.0），首页运行时显示错误版本；本地化工具 v0.2.1 beta 未提交未部署，线上仍是旧版。

（详细证据与改进建议见正文；行动优先级见 §7。）

## 1. 审核范围与方法

- 四个 Web 工具：`tools/{character-editor,map-editor,idle-lab,localization-lab}/`（app.js 合计约 40 万字符），全部通过 `node --check` 与各自 `self-check.js`。
- AI 基建：`yami-mcp/server.js`（671 行，MCP stdio）、`自动刷新方案/`（方案文档 + 补丁 + 引擎源码仓库实现）、`yami-plugins-bulider/`（skill）、`AI/reasonix.toml`。
- 引擎实证：安装目录 `resources/app/`（dist/index.html 341,958B、assets/index.js 5,359,505B、main/main.ts、dist-electron/main.js）；源码仓库 `D:\Documents\GitHub\2`（git log、Project/Script/module/autoreload.ts、build 链）。
- 游戏工程实证：`D:\new-game`（manifest 2.8MB、Assets 4716 文件、91 个 .event、16 个 .ui、53 个 .ts 插件脚本；Dist 编译产物 8/14 新于源文件，tsgo watch 正常）。

## 2. 引擎侧实际生产痛点盘点（报告的问题域）

按「实际生产过程中每天会遇到」的标准，整理出 10 个痛点，第 3/4 章的工具审核围绕这些痛点展开：

| # | 痛点 | 现场证据 | 现状 |
|---|---|---|---|
| P1 | 外部修改数据文件后编辑器不刷新，必须重开项目 | 经验笔记 §4 源码级结论（Meta.redirect 不重读内容） | 已有方案（自动重载），**未部署** |
| P2 | AI/工具写盘与编辑器内存未保存改动互相覆盖 | 编辑器无锁；工具写盘绕过编辑器 | 自动重载有编辑器侧三重保护；工具侧无任何协调 |
| P3 | GUID 手工管理易错（生成/查重/引用） | 经验笔记 §3「GUID already exists」事故 | MCP 有 generate_guid/validate_project；web 工具不涉 GUID |
| P4 | 本地化工作量大、规则隐晦（只有进 UI 文本才本地化） | 机制文档 §8.8 源码实证；195 条叶子 / 缺翻译 124 | 快速本地化 v0.2.1 已覆盖大部分 |
| P5 | 数值调参没有反馈闭环（改完只能进游戏试） | 无自动化测试；挂机机制尚未实装 | 挂机验证台已覆盖，模型边界见 §3.3 |
| P6 | 地图数据靠 Excel 手工维护 | 五表 Excel + 200 行格式残留 | 地图编辑器 + 地图JSON读取指令；阶段 3 未完成 |
| P7 | 掉落/属性逐个文件手改 | .actor 明文 JSON | 角色编辑器已覆盖 |
| P8 | 大文件编辑体验差（132KB 事件 / 157KB UI） | @4 玩家控制 132KB、基础角色AI 101KB | 无工具；引擎无大纲/搜索定位 |
| P9 | 音频未压缩（WAV 10-18MB/个）拖垮部署包 | 实测 8 个 WAV 均 >10MB | 无工具；引擎内置 ffmpeg.dll 未被利用 |
| P10 | 引擎升级覆盖 resources/app，补丁/文档需重打 | 安装目录 7/25 构建；文档路径是旧架构 | 无补丁管理机制 |

## 3. 四个 Web 工具逐一审核

### 3.1 角色编辑器（v0.7.2）

已覆盖痛点：P7（掉落/属性编辑）。
亮点：备份-回滚闭环（createBackupBatch 先备份校验再写、失败自动 rollbackRoles）、外部修改保护（写前 `current !== role.raw` 即中止）、三种写入模式固定顺序。

问题（源码实证）：

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 高 | 写回格式不仿生：`JSON.stringify(data, null, 2) + '\n'` 恒为 LF + 尾换行；引擎原生 .actor 是 CRLF + 无尾换行（实测 @1 通用英雄角色 1417 个 CRLF、0 个裸 LF、文件以 `}` 结尾） | app.js:1886 | 移植 localization-lab 的 `serializeLike`（按原文件 EOL + 尾换行仿生写回；`createBackupBatch` 已读过 `role.raw`，可直接作格式模板），否则每次保存全文件 diff、git 噪声、编辑器侧"未保存"状态混乱 |
| 高 | **手动「重新扫描」/换工程静默清空全部未保存草稿**：`scanProject()` 开头无条件 `state.drafts.clear(); state.pending.clear()`；自动同步路径有保护（pending>0 跳过），但「重新扫描 ↻」按钮与「选择工程」直调 scanProject 无确认 | app.js:740 + 1959 | 扫描前 pending>0 时弹确认（"有 N 个角色未保存"），与自动同步共用保护逻辑 |
| 中 | `quantity` 序列化三元两分支完全相同（`entry.min === entry.max ? clampInteger(entry.min) : clampInteger(entry.min)`）——范围掉落的 quantity 永远写 min，疑似复制粘贴 bug | app.js:314 | 明确 quantity 语义：按期望数量写 `(min+max)/2`（与总价值口径一致），或删掉死代码加注释 |
| 中 | 无法识别 ID 的掉落条目保存时被**静默删除**：`parseDropList` 用 `.filter(e => e.id)` 丢弃且不警告，保存时整列表重写 | app.js:307 + 1719/1741 | 统计被丢弃条目并 toast「原列表含 N 条无法识别项，保存将移除」 |
| 中 | 角色/物品列表全量 innerHTML 重建 + 头像全部重绘（无行级复用）；5000+ 文件工程上千物品时会卡 | app.js:1019-1027 / 1433 | 行级增量更新/可视区渲染；头像 canvas key 复用 |
| 中 | fallback 模式批量下载：不同目录同名角色互相覆盖；`saveAll` 连续触发 N 次下载被浏览器拦截；文件名无路径信息无法放回工程 | app.js:1890 + 1923-1927 | 下载名带相对路径（`/`→`__`，与本地化工具一致），批量逐个确认或打包 zip |
| 低 | 备份文件名 `${guid||'actor'}_${basename}.bak`：无 GUID 且同名角色同批次备份互相覆盖 | app.js:1807-1809 | 无 GUID 时用路径哈希前缀 |
| 低 | 上次角色记忆键只按目录名（`loot-smith-last-role:${rootHandle.name}`），同名目录工程串台 | app.js:1031 | 用完整路径做键 |

### 3.2 地图编辑器（v0.7.0）

已覆盖痛点：P6（Excel/JSON 双向、10×10 工作台、批量编辑、撤销重做、错误定位）。
亮点：Schema 铁律（`-1` 语义、icon 大值不截断、方向映射）与 HANDOFF 16 项改造清单已落地并自检通过；工程同步（FSO + 轮询回退）。

问题：

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 中 | 输出仍是「下载 JSON」单出口；部署到游戏工程（复制到 Assets + 让引擎重建 manifest）需手工完成，且与引擎内置 Excel 读取事件（`!0读取excel`）并存形成双数据源 | 项目实测：`地图格.a844f43cb6d37d6f.json` 与 `地图格.xlsx` 同时在用；MEMORY §6 阶段 3 未做 | 完成阶段 3：统一入口事件改调 `地图JSON读取` 指令，废弃 Excel 五事件；或在编辑器侧提供「导出后一键同步」说明 |
| 中 | **自动重扫会重建检查器打断输入**：`scheduledRescan → renderAll → renderInspector` 整表 innerHTML 重建；用户在检查器输入怪物 GUID 时，外部文件变化会把输入框重建、焦点/字符丢失 | app.js:490-498 + 713-722 | 重扫前若焦点在 `#cell-form` 内，记录 r/c 与输入值，重扫后恢复焦点与值 |
| 中 | 多格粘贴越界被**静默裁剪**（`if (r < ROWS && c < COLS)` 跳过无提示）——把 4×4 区块粘到右下角会悄悄少贴几格 | app.js:1272-1307 | 粘贴后 toast「N 格超出边界已跳过」 |
| 中 | 工作表识别用 `sheet.name.includes(keyword)` 模糊匹配，「名称」与「怪物名称」并存时会静默选错表 | app.js:1332-1334 | 精确匹配优先，多匹配时报错让用户选 |
| 低 | 下载文件名硬编码「地图格.json」；工程实际文件是 `地图格数据.<16hex>.json` 且需登记 manifest，用户每次导出要手工改名/放回/登记 | app.js:1493 | 从已加载工程读出目标文件名预填；导出成功提示给出「放置路径 + manifest 登记」引导 |
| 低 | 多文件拖放只导入第一个（`files?.[0]`） | app.js:1737 | files.length>1 时提示 |
| 低 | 10×10 外「格式残留」判定不认 ExcelJS `{richText:[]}` 空富文本，可能误报 | app.js:1350-1363 | 补判 richText 空对象 |
| 低 | 版本号漂移：HANDOFF 写 v0.6.0、version.json 0.7.0、画布修复未提交 | git status | 定版 + 提交 |

### 3.3 挂机验证台（v0.1.0 beta，未完成，需求待定）

已覆盖痛点：P5（数值模拟/A-B 对比/全图体检/流程推演）。
亮点：模型与游戏实测对齐（阈值命中无下限、SPDTime 间隔、死亡即结束、每级 +1 成长）；固定随机种子保证 A/B 可复现；只读工程。

问题：

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 高 | **版本标注**：工具尚未完全做好（具体需求待定），版本应标注 beta；此前 HANDOFF 写 v0.9.0 与 version.json 的 `0.1.0 beta` 互相矛盾 | tools/version.json vs HANDOFF-IDLE-LAB | 按用户拍板：version.json 定 `0.1.0 beta`（需求明确后再升正式版）；发布前核对 version.json |
| 中 | **单关模拟可冻结页面数秒**：`simulateStage` 全同步主线程，「重复样本」上限 2000，低伤害配置下每样本战斗循环最长 3600 秒上限，2000 样本 = 数万次循环期间 UI 完全卡死；`runAll` 已限 100 样本，**单关没限** | app.js:212-251 + index.html:69 | 单关同样限样本或 rAF/worker 分片；大样本加预计耗时提示 |
| 中 | 流程推演同步密集模拟：while 循环（guard 1000）内每次 evaluate 都是一次最多 80 样本的完整 simulateStage | app.js:1100-1170 | 按 (关卡, 玩家, 升级) 缓存结果，或分片渲染进度 |
| 中 | scanProject **串行**读角色文件且失败静默跳过：逐个 await（角色编辑器是 40 并发），用户只看到「已读取 N 个角色」，不知道有角色被跳过 | app.js:538-544 | 批次并发（16~24），跳过数显示在 toast/状态栏 |
| 低 | expectedGold 的 dropRate 钳制与角色编辑器不一致：`clamp(finite(dropRate,1),0,1)` 会把百分数 50 钳成 1（=100%），金币期望高估；角色编辑器对 >1 且 ≤100 会先 ÷100 | app.js:364 | 统一两工具的 dropRate 归一化口径 |
| 低 | importMapFile 只校验 10×10 形状不校验 GUID/等级/权重，坏数据被 `finite()` 静默吞掉 | app.js:1178-1187 | 复用 map-editor 的 validateGrid 语义并报错 |
| 低 | scanProject/scanProjectFiles 约 60 行重复（manifest 读取、actors 循环、mapEntry/iconEvent 查找等） | app.js:528-645 | 抽公共函数 |
| 中 | 模型边界：无挂机机制实装（模拟的是目标态）、经验按单人计、金币只算期望值、流程推演仅一种攻击升级 | HANDOFF-IDLE-LAB §13 | 按 §14 路线：游戏内校准日志 → 挂机循环定案 → 构筑快照导入 → 经济价值表 |

### 3.4 快速本地化（v0.2.1 beta，未提交未部署）

已覆盖痛点：P4（本地化全流程）。
亮点（从 HANDOFF-LOCALIZATION 与自检实测确认）：三层信号准确算法（界面显示路径白名单 + setText 占位模板排除 + 字符串特征）、复刻引擎打包算法只扫已引用资产、open-yami 引擎格式 Excel 双向、serializeLike 仿生写回、增量刷新（写完不重扫）、备份还原面板、富文本展示/源代码编辑双态。E2E 全过（含夹具噪声路径）。

问题（源码/文档实证）：

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 高 | **「数据属性」sheet 的 Excel 导入在浏览器实际路径上是坏的（静默失败）**：`confirmImport` 两处（:1782、:1819）只用 `state.scan.candidates.find(c => c.id === row.id)` 查候选，`attributeCandidates` 不在查找范围 → 数据属性行走到 `if (!candidate) continue` 被跳过：资产文件里的硬编码名称/描述**不会被替换成 `<ref:ID>`**，但 localization 条目照常创建（且落到「其他」文件夹）→ 用户以为本地化成功，游戏里仍是硬编码中文。`renderImportPreview` 的孤儿判定（:1721-1727）同样只查 scan.candidates，数据属性行被误标为「孤儿修复」。单条「本地化」按钮（:1873）反而正确查了两张表。现有 E2E/verify-real2 只测纯函数核心，绕过 DOM 导入路径，所以没拦住 | app.js:1782、1819、1721-1727 对比 1873 | 三处改为 `[...state.scan.candidates, ...state.scan.attributeCandidates].find(...)`；并补一条走真实 DOM 导入路径（数据属性 sheet）的回归测试 |
| 中 | 界面候选与数据属性候选**共用同一 ID 空间**：同 normalized 文本同时出现在界面路径和属性路径时拿到同一 ID，导入时第二张 sheet 被「幂等跳过」或「原文不同」报错，属性侧被静默漏掉 | app.js:1231-1235 | 按 `(normalized, 来源域)` 分键，或给属性候选 ID 加独立命名空间 |
| 中 | `collectAssets` 完全串行读文件（逐文件 await 无并发批次）；GAME-20240905 2501 资产单次全量扫描可达数十秒，手动重扫/备份还原都整工程重扫 | app.js:1054-1080 | 按类型分批 8~16 并发读 |
| 中 | 5 秒轮询**逐条串行 getFile() 全部资产**（数千条）且无重入保护：大工程单轮 >5s 时并发轮询堆积，页面挂机时持续数千次文件系统调用 | app.js:2111-2125 + 1108-1109 | 轮询只盯 manifest/attribute/localization/config 等少量元文件；加运行中互斥标志 |
| 低 | 搜索框每击键全量重渲染（750 条已本地化视图 + 富文本递归渲染），无防抖 | app.js:2207 | 加 150-200ms 防抖 |
| 低 | 「立即备份」备份全部 watchPaths（数千文件）无进度、失败静默跳过、目录无限累积 | app.js:2034-2053 | 限制范围（配置 + localization）或给进度条 |
| 低 | 还原中途失败无自动回滚（已写文件半还原，仅靠「还原前」快照手工恢复） | app.js:2055-2085 | 失败时自动用「还原前」快照回滚 |
| 低 | 合并候选跨位置校验用第一个位置的原文：同文本不同文件带不同标签（`<color:FF0000>攻击力</color>` vs `攻击力`）时第二个位置校验失败 → 整个导入被误中止 | app.js:696-711 | 按 `location.raw` 分别校验 |
| 低 | 导入语言列识别过宽：表头不在 META_COLS 的任意列被当语言列写进条目 | app.js:1637-1675 | 只接受 `config.languages` 集合内的列名 |
| 中 | v0.2.1 beta 全部改动未提交未部署：本地代码（app.js 等 4 文件 + version.json）与线上 GitHub Pages 不一致；hub 卡片兜底仍显示 v0.1.0 | git status；hub index.html | 用户验收后按用户指示提交部署；跑一次 `node tools/bump-version.js` 更新 ?v= 缓存参数 |
| 中 | 已知漏判：setText 之外的运行时覆盖（脚本直接写元素 content）无法静态识别——完工工程界面候选仍有 121 条 | HANDOFF §10、MEMORY §5 | 长期：扫描自定义指令/对象脚本里对 UI.Text.content 的写入点（AST 级正则匹配 `\.content\s*=` 等），把被写元素加入占位模板排除集 |
| 低 | fill 写回不支持清空为「」；导入预览不支持勾选排除个别行 | HANDOFF §11 | 用户拍板交互后补 |

## 4. AI 基建审核（DANJUAN TOOLS 四件套）

### 4.1 yami-mcp（server.js v0.1.0）

实测：`node --check` 过；`node test.js` **10/11 过**，失败项为「list_scripts：期望 51 个 .ts，实际 53」——测试硬编码了会漂移的项目统计。

问题清单（源码实证）：

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 高 | **MCP 未接线**：README 声称"项目根 .mcp.json 已注册"，实测 `D:\new-game\.mcp.json` 不存在（项目根、DANJUAN TOOLS、用户目录、.reasonix 等候选位置全查过，全项目 grep `mcpServers` 零命中）→ AI 会话根本没有这些工具 | 文件系统实测（§8） | 重建 .mcp.json（内容照 README）；文档与磁盘状态要保持一致 |
| 高 | **README 核心前提不成立**：README 与 server.js 头注释声称「MCP 写入后编辑器聚焦即自动重扫刷新」——但聚焦重扫对**已存在**文件只走 Meta.redirect（不重读内容），只有**新文件**会被识别；且若编辑器内存有未保存改动，下次保存会用旧内存**覆盖 MCP 的新写入**，双向覆盖无任何检测 | README.md:8、server.js:8-9 vs Yami经验笔记.md §4 实测结论 | write_resource 落盘前读磁盘旧内容与上次读取比对，不一致时告警；README/头注释改口：「新文件聚焦即识别；已存在文件需重开项目或配合自动刷新补丁」 |
| 高 | **validate_project 引用完整性≈失效（海量误报）**：`collectRefs` 把内容里**任何** 16hex 字符串当引用，但「已知 GUID」只收集**文件名** GUID——属性 key（4147 条）、presetId（1815）、motion id（505）、easingId（519）、枚举 stringId（339）、eventType/type（224）等全部住在 `Data/*.json` 或父文件内部，不是独立文件 → 子代理实跑 MCP 报 **5584 条 dangling-ref**（我复刻口径实测 9589 条/1305 个 GUID，90%+ 是误报）；且 `ok` 只看 error、warning 不改变结果，test.js 只断言 ok+duplicateGuids，噪音被完全掩盖 | ref-audit.js 实测（见 §8）+ server.js:240-263、319-334、348 | 「已知 ID 集」扩充：文件名 GUID + Data/*.json 内嵌 ID（variables/attribute keys/groups/enumeration/easings/teams/localization 叶子）+ 数据文件内部声明（.anim motions、.ui/.scene presetIds、事件 id）；或按**字段类型白名单**只校验已知引用字段；并把真悬空按字段分级（eventId/sceneId 等文件引用 = error，属性/枚举 = warning）；test.js 加「warning 数 < 阈值」断言防回归 |
| 高 | **无法注册脚本**：`write_resource` 只接受 DATA_TYPES，`Data/commands.json`/`plugins.json` 不可写；`create_script` 生成 .ts 后无任何注册步骤 → AI 产出的自定义指令/插件不会出现在编辑器里，工作流断链 | server.js:564-571（TYPE_BY_EXT['.json'] 未定义）、create_script case | 新增 `register_command`/`register_plugin`/`unregister_*` 工具（安全读写 Data/commands.json、plugins.json 对应条目）；或在 create_script 里加 register 参数一步到位 |
| 高 | **路径穿越（任意文件读写）**：`normalizeRelPath` 只归一化反斜杠、剥 `./`，**既不 resolve 也不拒绝 `..`**；五个工具（read_resource :544、write_resource :597-600 且 `mkdirSync(recursive)`、create_script :501-504、parse_plugin_meta :488、validate_resource :559→:270）直接 `path.join(ROOT, rel)` 无包含性校验 → `../../` 可逃逸项目根，read 可读任意数据扩展名文件，write 可建目录写任意位置（如覆盖用户目录 .openyami 配置） | server.js:469-471 + 上述五处 | 统一入口 `resolveInRoot(rel)`：`path.resolve` 后校验前缀在 ROOT 内，越界返回错误；test.js 补 `../../escape` 用例 |
| 中 | write_resource 只查文件名 GUID 格式与文件内 presetId 唯一，**不查全工程 GUID 冲突**（新文件与已有文件撞 GUID 会触发引擎 "GUID already exists" 事故） | server.js:564-605 vs validateProject 才有全局查重 | write 前调用 collectAllGuids 查重，冲突即拒绝 |
| 中 | **presetId 唯一性只查文件内**，引擎要求**全局唯一**（by-id 寻址）：AI 复制 .ui/.scene 节点产出跨文件重复 presetId 时编辑器不报错，运行时 createElement/deleteElement 寻址错乱难排查 | server.js:294-307、581-592 vs 机制文档 §4.1 | validate_project 增加全局 presetId 去重（收集所有 .ui/.scene 节点 presetId 与文件名 GUID 同表查重） |
| 中 | 无 RLE 防护：read_resource 原样返回 terrains/code，AI 若读-改-写 .scene 可能无意破坏 RLE 串（JSON 字符串改写无校验） | server.js README 仅口头警告 | write 时对 RLE 字段做「与磁盘版本逐字符一致否则拒绝」校验；或提供 codec 编解码工具 |
| 中 | 缺删除/重命名/移动工具（AI 改资源名会留下旧文件 + manifest 脏条目）；缺 `find_refs(guid)` 反向引用查询（AI 改资源前不知道谁引用它——引擎无此功能，MCP 是最佳落点） | 工具清单 | 新增 delete_resource（先查引用为零）、rename_resource（同 GUID 改名）、find_refs |
| 中 | compile_check 用 node 跑 tsc.js（1-2 分钟全量）；引擎本身用 TS7 原生 tsgo（快得多） | server.js:521-537 | 优先探测 `node_modules/@typescript/typescript-win32-x64/tsgo.exe`，回退 tsc.js |
| 低 | 测试基线硬编码漂移数字（51/53） | test.js:123 | 断言改成「≥N 且四类分组总数=总数」等不变量 |
| 低 | cdp_eval 仅占位：编辑器窗口级刷新（scene/ui/anim 窗口）恰好是自动重载方案的盲区，CDP 是补上这块的正交手段 | server.js:606-607 | 结合自动重载落地后，用 `--remote-debugging-port` + `Directory.update()`/窗口重开实现窗口级刷新，作为可选增强 |

### 4.2 自动刷新方案（方案.md + patch + 引擎源码仓库实现）

状态链：方案设计（8/13）→ 源码仓库实施（8/13，8 处改动）→ **未提交、未构建、未部署**（安装引擎 7/25 构建无 autoreload）。

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 高 | 功能未达用户：安装版 `dist/index.html` grep 无 autoreload；仓库工作区有 9 个修改文件 + autoreload.ts 未跟踪、无任何提交 | 安装目录 grep=False；repo git status/log | ① 立即提交仓库改动；② `pnpm install && pnpm run dev` 先本地验证；③ `pnpm run build:all`（= build:vite + build:electron）重建并替换安装目录 resources/app（或等官方发布）；④ 把「升级重打清单」固化成脚本 |
| 高 | **两份 .js 交付物是旧架构死代码**：`auto-reload.patch.js`/`autoreload.module.js` 依赖的 `window.Data/File/Scene/Updater/EventEditor/GlobalPath/File.route/File.root` 在新 bundle 中**全部不存在**（实测 `window.Data` 等计数 = 0，EventEditor 仅剩 ESM 导入符号）；`autoreload.module.js:2` 的 `const require = window.__nodeRequire` 是死代码（全文从未调用），随后裸用未定义的 `FS`/`Path` → 新架构下直接 ReferenceError；安装指引指向的 `html/head.html`、`Script/patch/` 也都不存在 | 新 bundle grep 计数（§8）+ autoreload.module.js:2、79、116 | 两份 .js 标注「仅适配旧架构（2026-07-25 之前安装版），已废弃，以仓库 autoreload.ts 为准」；指南删除「内容一个字都不用改」的复制指令 |
| 高 | **dev 模式必死 bug**：`File.route('Assets')` 在 `import.meta.env.DEV` 时返回 `/local-file/?path=...` URL，`fs.existsSync/watcher` 拿到 URL → 监听永不启动；官方验证指南却让用户在 dev 下验证 → 会得出"功能没生效"的错误结论 | file-system-core.ts:360-371 vs autoreload.ts:83-84,216-217 | 改用 `File.path('Assets')`（fs 操作正确 API）；或 dev 分支回退真实路径。改完在 dev 与 build 各验一次 |
| 高 | **tickRoot 每 2 秒重启 watcher（三处同款：patch.js:266-272 / module.js:66-71 / autoreload.ts:73-78）**：`tickRoot()` 比较 `File.root !== this.watchedRoot`，但 `watchedRoot` 存的是 `File.route('Assets')`（Assets 路径），两者**永不相等** → 每 2 秒 stopWatcher（**清空全部防抖 pending 定时器**）+ startWatcher 一次。后果：① 控制台每 2 秒刷「监听已启动」；② 300ms 防抖窗口跨过 tick 的外部修改被清掉、重载丢失，只能靠 5 秒轮询兜底（实际延迟达 5-8 秒，违背「≤0.3s」承诺）；③ Windows 上 fs.watch 重建有事件缝隙，刚重启后的保存可能完全不触发 | autoreload.module.js:66-71、78、90-97 | 改比较 `File.route('Assets') !== this.watchedRoot`（或单独存项目根快照）；`stopWatcher` 不应清 pending（让存量防抖跑完）；加回归断言「运行 10 秒内 watcher 重启次数 = 0」 |
| 中 | 新建文件盲区：外部新建 .event → `Data.manifest.pathMap` 无该 meta → 静默跳过（需焦点重扫才进内存）；删除文件无处理（Data 残留旧条目） | autoreload.ts:123-124 | watcher 回调对 unknown 文件触发 `Directory.update()`（引擎自带增量扫描）补上新建/删除两条路 |
| 中 | 窗口级刷新盲区：scene/ui/anim/particle 窗口不热重载，仍要手动关开 | 方案.md §5 已知限制 | 二期：结合 §4.1 的 CDP 通道或引擎侧窗口级 reload 钩子 |
| 低 | DANJUAN TOOLS 里新旧两套方案文档并存（旧方案.md 面向旧架构 head.html；实施指南 §7 面向新架构），且**实施指南 §2 的安装版改动清单全部落在不存在的文件上**（html/head.html、html/tools.html、Script/module/settingconfig.js、app 根 Locales）——新架构下 §2 无任何文件可改 | 文档对比 + 新布局实证 | 删除或改写指南 §2（标注「仅 2026-07-25 前旧安装版适用」），只保留 §7 仓库实施路径，并补「发布安装版需 `pnpm run build:all`」 |

### 4.3 yami-plugins-bulider（skill）

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 中 | **SKILL.md 存在会误导 AI 产出坏代码的事实错误**：① 全局插件模板用 `onStart()` 表示「游戏就绪后触发」——按 eventTypeMap（`startup→onStartup`、`autorun→onStart`），应为 **`onStartup()`**，写 onStart 的启动钩子实际永不触发；② `onBeforeLoad(data, define)` 签名错误（define 只在 onBeforeSave 有意义）；③ 模板/参考用 `@lang zh-CN` 与项目实际脚本 `@lang zh` 不一致（源码 plugin.ts:1049-1054 按前缀匹配，两写法都能解析，属风格不一致但会污染 AI 学到的模式） | SKILL.md:157-158、197-199；api_reference.md:158 | onStart→onStartup；onBeforeLoad 去 define；统一 `@lang zh` |
| 低 | 目录/技能名拼写 "bulider"（应为 builder），已随 .reasonix/skills 复制进项目，改名需同步两处 | 目录名 | 统一改名（改后同步 .reasonix 副本） |
| 低 | api_reference.md 末尾残留「全局 Kilo skill 应写入用户目录」无关文本；@number 默认值表述（0/decimals 10）与机制文档（''→@default）不一致 | api_reference.md:73,264 | 清理残留；以引擎 plugin.ts parseMeta 实测为准统一表述 |
| 低 | api_reference.md 顶部路径仍是旧架构（resources/app/Script/plugin.js、resources/app/Locales/——实际是 dist/Locales/） | api_reference.md:7-21 | 更新为新布局：安装版看 dist bundle，源码看仓库 Project/Script/plugin/plugin.ts |

### 4.4 AI（reasonix.toml）

| 严重度 | 问题 | 证据 | 改进建议 |
|---|---|---|---|
| 中 | 权限配置只开放 yami-tools 写权限；AI 要改游戏工程（写 .event/插件）时没有对应授权路径，与 yami-mcp 的「AI 直接写游戏工程」定位不匹配 | AI/reasonix.toml allow_write | 明确 AI 写 D:\new-game 的策略：只经 MCP 写（MCP 自带校验）+ 白名单目录，还是继续人工代写 |
| 低 | reasonix.toml 里两条 allow 命令是旧统计口径（Script/*.ts 行数等），口径与当前 53 个插件脚本等事实漂移 | reasonix.toml | 无碍，可随文档更新 |

## 5. 痛点 → 解决方案对照表（含"已有功能如何改进"）

| 痛点 | 已有功能 | 缺口 | 解决方案 |
|---|---|---|---|
| P1 编辑器不刷新 | 自动重载（仓库实现未部署） | 部署 + dev bug + 新建文件盲区 + 窗口级盲区 | 按 §4.2 三条修 |
| P2 写盘冲突 | 自动重载编辑器侧三重保护；角色编辑器外部修改中止 | 工具侧不知道编辑器开着、有没有未保存改动 | 工具写回前读 `Lootsmith Backups` 之外的哨兵不可行——正解是：编辑器侧（自动重载）保护 + 工具侧「写前重读比对」强化（localization-lab 已有）推广到全部写回路径 + 文档约定「用工具时先 Ctrl+S 编辑器」 |
| P3 GUID 管理 | MCP generate_guid/validate_project | MCP 未接线；write 不查全工程冲突 | §4.1：接线 + write 前查重 + find_refs |
| P4 本地化 | 快速本地化 v0.2.1 | 未部署；setText 外覆盖漏判；fill 不能清空 | 部署 + 脚本写入点扫描 + 交互补全 |
| P5 数值调参 | 挂机验证台 | 模型是目标态；无校准闭环 | 游戏内校准日志指令（自定义指令记录战斗统计写 Save/JSON）→ 验证台读取校准 |
| P6 地图数据 | 地图编辑器 + 地图JSON读取指令 | 阶段 3 未完成；双数据源 | 统一 JSON 入口 |
| P7 掉落编辑 | 角色编辑器 | 仿生写回缺失；只覆盖 .actor | serializeLike 移植；.skill/.item/.equip 编辑可作为四期扩展 |
| P8 大文件编辑 | 无 | 无 | 引擎侧：事件列表大纲/跳转（上游建议）；MCP：事件静态分析工具（挂起无恢复路径检测、label/jumpTo 校验、禁用指令统计）——纯文件层即可做 |
| P9 音频体积 | 无 | 无 | 本地 ffmpeg（引擎自带 ffmpeg.dll 不可直接调用则用系统 ffmpeg/wasm）批量 WAV→OGG 转换脚本 + manifest 更新，做成 yami-tools 第五工具或 MCP 工具 |
| P10 升级覆盖 | 无 | 无 | 补丁管理脚本（备份→应用→验证清单），放进 DANJUAN TOOLS；文档路径统一改为源码仓库映射 |

## 6. 文档体系问题（连带修正）

- `Yami引擎机制.md` §2.2/§10 与 `项目架构文档.md` §2.2 的路径（`resources/app/Script/`、`html/`、`css/`、main/main.js 1.8k 行）描述的是旧引擎架构；当前安装引擎 = vite 打包（dist/index.html 342KB + assets/index.js 5.3MB + main/main.ts + dist-electron/main.js），源码在 `D:\Documents\GitHub\2\Project\Script/**/*.ts`。逐一实测确认旧路径全部不存在：`html/head.html`、`Script/plugin/plugin.js`、`Script/main/main.js`、`Script/patch/`、`app 根 Locales/`。
- **window 全局认知整体过时**：机制文档 §1.1 说「编辑器全局是挂在 window 上的单例对象（Data/File/Scene/Updater/EventEditor/SettingConfig…）」——新 bundle 实测 `window.Data/File/Scene/Updater/EventEditor` 计数全部 = 0（业务单例已改为真 ESM import，只有 `window.on/off/dispatchEvent/__nodeRequire` 等 DOM 工具残留）。但**数据链路 API 语义原样保留**（Meta 构造后仍调 `updateGlobalEvent`/`registerScenePresets`/`registerUiPresets`；`manifest.changes` 仍是普通数组、引擎自己调 `.includes(meta)`），所以自动重载的三重保护设计在新架构下依然成立——只是必须以 ESM import 形式接入。
- 建议：在机制文档 §1.1 加一张「旧安装包路径 → 新源码仓库路径」映射表（`Script/plugin/plugin.js` → `Project/Script/plugin/plugin.ts`；`html/head.html` → `Project/html/head.html`；行号引用以源码仓库为准），并改写 §1.1 的全局单例段为 ESM 模块表，避免后续 AI 打错补丁。
- 行号引用全部会随引擎升级漂移——文档自己已声明「以文件+函数名为准」，建议工具链（MCP/skill）统一引用函数名而非行号。

## 7. 行动优先级路线图

**P0（本周，直接消除日常痛感）**
1. ✅ 引擎仓库 autoreload 修复（代码层）：dev 模式 `File.route`→`File.path` bug、tickRoot 每 2 秒重启 watcher bug 已修复（autoreload.ts）；两份旧架构 .js 已标注废弃。**未提交、未构建、未部署（按约定等你拍板）**。
2. ✅ localization-lab「数据属性」Excel 导入静默失败已修复：新增 `findCandidateById`/`classifyImportAdditions` 纯函数，预览/导入共 4 处统一走新逻辑，self-check 新增回归用例；自检 + 浏览器 E2E（test-localize.js）全过。
3. ✅ 角色编辑器：重扫/换工程前草稿确认已加（`window.confirm`，pending>0 时拦截）；`serializeLike` 仿生写回已移植（按原文件 CRLF/尾换行写回）。
4. ✅ version.json：idle-lab 改回 0.9.0；5 个 HTML 的 version.json 引用补 `?v=` 缓存参数，bump-version.js 增加 version.json 查询串维护（此前改版本号会被浏览器缓存挡住）。
5. ✅ 重建 `D:\new-game\.mcp.json`（README 配置原样）；生效条件：Reasonix 重开会话或 `/mcp` 刷新。

**P1（两周内）**
6. MCP：路径穿越校验 + write 前 GUID 全局查重 + presetId 全局查重 + **validate_project 引用完整性去误报（扩充已知 ID 集）** + commands/plugins 注册工具 + find_refs/delete_resource + README「聚焦即刷新」改口。
7. 自动重载：新建/删除文件走 Directory.update()；窗口级刷新评估（CDP 或引擎钩子）。
8. 文档路径映射表 + 补丁管理脚本；MCP 测试断言改为不变量；SKILL 修复（onStart→onStartup、onBeforeLoad 签名、@lang zh、bulider 改名）。

**P2（中期）**
9. 阶段 3：地图数据统一 JSON 入口，Excel 退役。
10. 性能三件套：localization-lab collectAssets 并发读 + 轮询瘦身、idle-lab 单关模拟分片、character-editor 列表增量渲染。
11. 事件静态分析工具（挂起检测/引用图）并入 MCP。
12. 音频转换脚本/工具；挂机验证台校准日志闭环；skill 改名与清理。

## 8. 已验证证据清单（可复查）

- 安装引擎：`dist/index.html`（341,958B，无 autoreload）、`assets/index.js`（5,359,505B）、`main/main.ts`（23,987B）；全部时间戳 2026-07-25 21:06。旧路径逐一 Test-Path 均不存在（html/head.html、Script/plugin/plugin.js、Script/main/main.js、Script/patch/、app 根 Locales/）。
- 新 bundle 关键计数（[regex]::Matches 精确计数）：`autoreload`=0、`AutoReload`=0；`editor_loaded`=8、`updateGlobalEvent`=6、`registerScenePresets/registerUiPresets`=6/6、`dirchange`=8、`manifest.changes`=3；**`window.Data/File/Scene/Updater/EventEditor/GlobalPath/File.route/File.root` 全部 = 0**（window 业务全局消失，改真 ESM）。
- 源码仓库（D:\Documents\GitHub\2，HEAD b5c50a34 @ 07-29）：`Project/Script/module/autoreload.ts` 存在（7,904B，**git 未跟踪**）；9 个文件已改未提交（Locales×2、settingconfig/index.ts、schema.ts、head.html、index.css、index.html、package.json、build-module.js）；`module-init.js:481` 已含 autoreload import；repo 自身 dist 为部分构建（无 index.html/assets，无 autoreload）；`File.route` 的 DEV 分支返回 `/local-file/?path=` URL（file-system-core.ts:360-371）；`File.path` 才是 fs 用真实路径 API。
- yami-mcp：`node --check` 过；test.js **10/11 过**（唯一失败 = list_scripts 51 vs 53 漂移，test.js:123）；实跑 `validate_project` → `ok=true, files=4698, duplicateGuids=0, dangling-ref=5584`；我的复刻口径 ref-audit.js 实测 **9589 条悬空引用/1305 GUID**，按字段名分类：key=4147（属性）、presetId=1815、id=1496、easingId=519、motion=505、stringId=339、eventType/type=224 —— 90%+ 是 Data/*.json 内部 ID 误报；`D:\new-game\.mcp.json` 不存在（项目根/DANJUAN TOOLS/用户目录/.reasonix 候选位置全查过，全项目 grep mcpServers 零命中）；write_resource 不支持 .json（TYPE_BY_EXT['.json'] undefined）；`normalizeRelPath` 无 `..` 越界校验（server.js:469-471，五处 join 未包含校验）。
- 角色编辑器：app.js:1886 `JSON.stringify(role.data, null, 2) + '\n'`；实测 .actor 原生格式 = CRLF×1417、裸 LF×0、无尾换行（`@1 通用英雄角色.1ab20a44c47972b7.actor`）；app.js:740 `scanProject` 无条件清空 drafts/pending；app.js:314 quantity 死三元。
- localization-lab：confirmImport 三处只查 scan.candidates（app.js:1782、1819、1721-1727），对比 localizeCandidateNow（:1873）查两张表；verify-real2.js 只测纯函数核心绕过 DOM 层。
- 版本链：tools/version.json idle-lab="0.1.0 beta" vs HANDOFF v0.9.0 vs hub HTML 兜底 v0.9.0（运行时被 version.json 覆盖 → 现显示错误版本）。
- 游戏工程：Assets 4716 文件；`@4 玩家控制` .event 132KB、`基础角色AI` 101KB；8 个 WAV >10MB；Dist 编译产物（8/14 16:33）新于源文件（tsgo watch 正常）；`地图格.xlsx` 与 `地图格.a844f43cb6d37d6f.json` 双数据源并存。
- 四个工具自检全过：localization lab / map editor / idle lab self-check passed；四 app.js node --check 通过。
- 复现脚本：`D:\new-game\DANJUAN TOOLS\yami-mcp\ref-audit.js`（悬空引用构成审计，修复 validate_project 时可复用为基线）。

## 9. 修复记录（2026-08-15，P0 落地）

> 按用户指示实施 P0。改动全部**未提交、未推送、未部署**（遵守项目约定，等用户验收后指示）。

### 已修复
1. **localization-lab 数据属性导入静默失败**（`tools/localization-lab/app.js`）
   - 新增纯函数 `findCandidateById(scan, id)`（先 candidates 后 attributeCandidates）与 `classifyImportAdditions(scan, additions)`（返回 `{groups, orphanAdds}`）。
   - 预览区（原 :1720-1724）与 confirmImport 两处（原 :1782/:1819）统一改走新函数；数据属性行现在会正确替换资产文件为 `<ref:ID>` 且归入正确类型文件夹。
   - `self-check.js` 新增回归用例（界面候选/数据属性候选/孤儿三行分类断言）；`node --check`、self-check、浏览器 E2E（test-localize.js，本地 4173 服务）全部通过。
   - 缓存参数 `?v=20260815-localization-lab-14`。
2. **角色编辑器草稿保护 + 仿生写回**（`tools/character-editor/app.js`）
   - `scanProject` 增加 `force` 参数：`state.pending.size > 0` 且非强制时 `window.confirm` 拦截（覆盖手动「重新扫描」与切换工程两条路径；自动同步路径原有保护不变）。
   - 新增 `serializeLike(data, originalText)`（与快速本地化同一底线：按原文件 CRLF/LF 与尾随换行有无写回），`writeRoleModes` 由 `JSON.stringify(...,2)+'\n'` 改为 `serializeLike(role.data, role.raw)`——消除每次保存的全文件 diff。
   - `node --check` 通过；缓存参数 `?v=20260815-character-editor-16`。
3. **版本标注修正**（`tools/version.json` + 5 个 HTML + `tools/bump-version.js`）
   - idle-lab 版本定为 `0.1.0 beta`（用户拍板：工具未完成、需求待定，先标 beta；HANDOFF 的 v0.9.0 与 version.json 原 `0.1.0 beta` 的冲突已按 beta 定稿）。
   - 5 个 HTML 的 `<script src="…/version.json">` 补 `?v=` 缓存参数（此前 version.json 无缓存参数，改了版本号也会被浏览器/GitHub Pages 缓存挡住——这是"改了 version.json 但界面版本不变"的根因）。
   - `bump-version.js` 增加 `bumpVersionJson()`（负向环视排除互相干扰），发布前跑一次即可同时刷新资源与版本查询串。
4. **自动重载两个必死 bug**（引擎仓库 `D:\Documents\GitHub\2\Project\Script\module\autoreload.ts`）
   - dev 模式：`File.route('Assets')`（dev 返回 `/local-file/?path=` URL）全部改为 `File.path('Assets')`（恒返回真实路径），共 4 处（startWatcher/reloadFile/pollScan/startPoll）。
   - tickRoot：改为 `File.path('Assets') !== this.watchedRoot` 比较（原 `File.root !== watchedRoot` 恒真 → 每 2 秒重启 watcher 并清空防抖队列）；并加 `!File.root` 早退避免无项目时空转。
   - `node --check` 通过（Node 25 type stripping）。**未提交、未构建、未部署**——验证与发布按 P0-1 后半段：`pnpm install && pnpm run dev`（dev 现在能真正工作）→ `pnpm run build:all` → 替换安装目录或走官方发布，等你拍板。
5. **MCP 接线**：新建 `D:\new-game\.mcp.json`（README 原配置）。生效需 Reasonix 重开会话或 `/mcp` 刷新；重开会话后可用 `/mcp` 验证 `mcp__yami__*` 工具出现。
6. **旧交付物标注废弃**：`自动刷新方案/auto-reload.patch.js` 与 `autoreload.module.js` 头部加废弃说明（指向仓库 autoreload.ts）。

### 待办（P0 内剩余，需用户拍板/验收）
- 引擎仓库提交 + `pnpm run dev` 实测 + `build:all` 构建 + 安装版替换（涉及部署，按约定等指示）。
- localization-lab v0.2.1 beta 与本次修复的用户验收 → 提交 → GitHub Pages 部署（用户说「发」才做）。
- 角色编辑器仿生写回建议在真实工程保存一个角色验证 CRLF 保持（工具侧已过自检，真实写盘请按用户验证流程）。

## 10. 第二轮修复记录（2026-08-15，yami-tools 追加，未提交）

> 用户指示「重点是做 yami tools」后的追加修复，仍按约定未提交未推送。

1. **localization-lab 跨域候选 ID 冲突**（`tools/localization-lab/app.js`）：界面候选与数据属性候选按 normalized 共享同一 ID 时，`classifyImportAdditions` 原用 `find` 只取第一个候选 → 第二个域（数据属性）的资产文件不被替换。新增 `findCandidatesById`（返回两域全部匹配），`classifyImportAdditions` 合并所有匹配候选的 locations。self-check 新增跨域同 ID 用例。
2. **localization-lab 搜索防抖**：`filterQuery` 输入由逐键 `renderList()` 改为 180ms 防抖（750 条已本地化视图 + 富文本递归渲染逐键全量重绘会卡顿）。
3. **角色编辑器 quantity 死三元**（`tools/character-editor/app.js`）：`serializeDropList` 移除 `quantity` 字段——引擎「掉落物品」指令运行时用 `Math.randomInt(this.min, this.max)` 现算 quantity（指令 :139），存储里写 quantity 无人读、且原三元两分支相同。格式回归 README 的 `{type,id,min,max,dropRate}`。
4. 缓存参数 bump：localization-lab → `-15`、character-editor → `-17`。

验证：`node --check` 全过、localization self-check 通过（含新用例）、浏览器 E2E（test-localize.js）通过、git diff --check 无空白错误。待用户「git 上去」再提交推送。
