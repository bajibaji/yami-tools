# 地图编辑器生产力改造计划

> 基线：2026-08-11 当前仓库与 `D:\new-game\Assets\UI\地图\地图格数据.2c1c219af5b9e43a.json`
> 范围：只改 `tools/map-editor/`，不修改游戏工程，不引入新依赖

## 1. 现状结论

当前地图编辑器的数据转换链路基本成立，但交互模型仍是“逐格表单演示”：一次只能处理一个格子，误操作不能恢复，错误不能直接定位，导入会覆盖当前工作，真实图标值被错误合并，JSON 预览长期挤占编辑空间。这些问题共同决定了它不能承担高频地图制作。

真实地图数据包含 50 个命名地点、30 条怪物记录；图标值为 `-1`、`0..7`、`100..102`。界面必须完整区分这些值，不能再显示成“4+”。

## 2. 本轮目标

1. 把地图区域改成固定尺寸、带行列坐标和可缩放的 10×10 工作台。
2. 支持单选、Ctrl 切换选择、Shift 矩形选择、行列全选和全选。
3. 支持多格批量设置图标、通行、等级、怪物，以及用主格完整覆盖所选格。
4. 支持撤销/重做、内部复制/粘贴、方向键移动、Delete 清空。
5. 所有字段实时校验；错误格在地图上标记，错误列表可点击定位。
6. 导入、新建覆盖未导出修改前必须确认；离开页面时保护未导出修改。
7. JSON 预览改为按需打开，不占用常驻属性编辑区。
8. 对齐角色编辑器的顶栏、颜色、表面层级、按钮和状态栏语言。

## 3. 明确不做

- 不引入 React、Vue、状态库或图标依赖。
- 不拆分模块；当前逻辑规模仍可由一个脚本维护，先解决真实工作流。
- 不修改 JSON Schema、Yami 加载器或游戏工程事件。
- 不猜测图标在引擎中的正式枚举名；界面只按真实值和现有地点样本提供可辨识标签。

## 4. 验收标准

### 数据

- Excel 与 JSON 均可导入，导出仍为 10×10 根二维数组。
- `Passability` 方向映射保持第一位 `right`、第二位 `down`。
- `-1`、`0..7`、`100..102` 均可单独选择和辨识。
- 空数字、等级倒置、非法/重复 GUID、非正权重均即时阻止导出。

### 编辑效率

- 一次操作可选择整行、整列或矩形区域并批量修改。
- 任一数据修改可以撤销和重做。
- 单元格可以复制并粘贴到一个或多个目标格。
- 方向键可连续移动主选格；错误项点击后直接选中对应格。

### 安全

- 有修改时，新建、导入或离开页面会出现保护提示。
- 下载按钮只在已载入且校验通过时启用。
- 导出成功后当前内容被标记为已导出，脏状态清除。

### 界面

- 1366×768 下地图、属性栏和主要命令无需页面级纵向滚动即可使用。
- 900px 以下改为上下布局；地图允许局部横向滚动，控件不重叠。
- 属性表单使用完整宽度的怪物 ID 和独立数字字段，不再挤成五个窄列。

## 5. 最小自动检查

仓库保留 `tools/map-editor/self-check.js`。它直接复用编辑器导出的纯函数，至少检查空白网格、Schema 校验、通行方向、选择范围和历史快照，作为非框架回归入口。

