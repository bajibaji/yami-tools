# AI 助手功能复查报告

> 复查对象：本插件里的「AI 全能副驾」（ai-agent.js / ai-host.js / runtime/yami-mcp）
> 复查时间：2026-09-14 ｜ 复查方式：把相关代码从头到尾读两遍 + 实机跑回归 + 写小程序实测关键行为
> 本次只做检查，**没有改动任何代码**（唯一新增文件就是这份报告）

---

## 一、先说结论

**好消息**：这套东西的基础是扎实的。我把它自带的 30 套回归全跑了一遍，**30/30 全绿**；工具结果被裁剪时会如实标注并落盘留证据、编译不过会自动回滚、删文件要一次性令牌、消息序列坏了会自愈——这些容易糊弄过去的地方，它都做成了实打实的。

**坏消息**：还有 **6 个真问题**（会实际咬人的），另有 14 个中等和次要问题。其中第 1、2 条最值得马上处理：一个是安全保护其实没生效，一个是省上下文的功能在错误的时间点动手、而且界面上的说明是错的。

---

## 二、会实际咬人的问题（建议优先处理）

### 问题 1 ｜「编辑器里有没保存的修改，先别覆盖」这道保护，实际上从来没拦住过

**现象**：代码里写得很明确——AI 动工程文件之前，要先问编辑器一句「这个文件你手里有没有还没保存的改动」，有就拒绝。但真机上（官方预编译版 Open Yami）编辑器回答的是「我这个功能没有」（engineUnavailable），这段代码把「问不出来」当成了「没问题」，于是照样往下写。

**后果**：你在编辑器里改了某个文件、还没按保存，AI 又去写同一个文件，两边很可能互相盖掉。这是**数据丢失**级别的风险，不是体验问题。

**证据**：runtime/yami-mcp/server.js 第 1096-1106 行。其中第 1102 行 `if (!result || result.ok === false)` 这一段，在任何情况下都执行不到（我顺着编辑器桥的返回结构走了一遍：能「查到」的情况已经在第 1099 行返回了），所以「桥不可用」那条分支永远不成立，永远返回「可以写」。

**顺带查到**：宿主那边还有一道独立的检查（「编辑器里有没失焦的输入」），那道是好的、确实会拦。所以现在的真实情况是：**打了一半的字会被拦住，改了没保存不会被拦住**——而这两种情况在用户眼里是一回事。

---

### 问题 2 ｜自动压缩上下文：动手的时间点和界面说的完全不是一回事，而且压完还是超标

界面上写着「窗口 1M，占用到 80% 自动压缩」，README 也是这么写的。实际代码里另有一句硬性规定：**超过 6.4 万就压**（ai-host.js 第 415 行）。也就是说真实阈值是界面宣称的 1/12。

更麻烦的是压缩「保留多少」用的是 1M 的 16%，也就是 16 万——**保留的比阈值还大 2.6 倍**。我写小程序按真实参数跑了一遍（30 轮工具调用、每条工具结果按宿主自己的上限 2.4 万字符）：

| 压缩前占用 | 压缩保留 | 真实阈值 |
| :--- | :--- | :--- |
| 21.7 万 | **16.7 万** | 6.4 万 |

**后果**：
1. 用户以为还很宽裕（界面显示 22%/1M），其实早就越过真实阈值了；
2. 一次压缩之后仍然是 16.7 万，还是远超 6.4 万——每轮都会判定「该压缩」，只是被后面的守卫挡住（只剩一条消息可折时不压），结果是**长期停在已超标但不再动的状态**；
3. 如果真的再触发一次，就是「拿摘要去摘要」，越压越失真。

**证据**：ai-host.js 第 415-416 行（阈值取 64000）、context-meter.js 第 138 行（保留量按 1M 的 16% = 16 万算）。测试之所以全绿，是因为测试里把窗口调成 3000，阈值和保留量刚好同比例缩小，掩盖了这个比例关系（tests/test-context-meter.cjs 第 32 行有注释说明）。

**另外**：计算「超没超」时把工具定义的开销重复算了一次（ai-host.js 第 413-416 行：usage.tokens 在已经对齐真实用量时本就含工具定义，又作为 toolsTokens 传了进去），所以判定会比真实情况更早触发。

---

### 问题 3 ｜新建脚本 / 新建事件时，确认卡是没有「差异预览」的

说好的「所有文件写操作永远先预览并等待确认」，但**新建类**操作拿不出差异：

- create_script：预览返回的是「模板已生成」，没有新旧对比（runtime/yami-mcp/server.js 第 1140 行）。
- 我实际跑了一次工具调用确认：返回字段只有 ok,dryRun,message,script——**没有 diff，也没有 diffStat**。

**后果**：确认卡上只有一句「新建 xxx」，用户点「执行修改」时其实不知道要写进去什么。改文件有差异、建文件没差异，这条体验是断的。

---

### 问题 4 ｜「点了执行修改之后」这一段时间，会话没有上锁

/chat（正常发一句话）会给会话加锁，防止两件事同时改同一个对话。但**审批之后的续跑没有加锁**（ai-host.js 第 2212-2286 行的 /approve 分支，全程没有把 session.busy 置为 true，它是等最后才去调 runTurn）。

**后果**：这段时间里，另一个窗口（或编辑器里的另一次请求）如果也对着同一段对话发消息，就会和续跑同时进行。两边各往同一个消息数组里塞东西、各往同一个文件里写——**对话内容可能丢、顺序可能乱**。前端平时靠「忙碌」按钮挡着，但那只挡本窗口。

**证据**：ai-host.js 里 session.busy = true 只出现在 /chat 分支（第 2184 行），/approve 分支没有。

---

### 问题 5 ｜两个窗口同时用 AI 时，面板可能整个连不上宿主

面板启动时会去读一个本机令牌文件（%APPDATA%/DanJuanDevSuite/agent-token）。如果这次读失败（杀软拦截、权限、目录被占），代码会退回「重新生成一个随机令牌」（ai-agent.js 第 6-19 行）——但宿主那边认的是**文件里那个令牌**，于是：

- 面板发的每个请求都被宿主判定为「令牌无效」；
- 面板会自动去拉起新宿主，可新宿主读到的还是旧令牌，仍然对不上；
- 结果就是：**面板永远起不来，而且不会告诉用户为什么**（报错是「请求失败」，跟令牌毫无关系）。

**次要问题**：ai-agent.js 里其实有两套令牌（一套给宿主、一套给界面本身），ensureHost() 里那段「没有令牌就新建」的兜底逻辑（第 295-298 行）在今天已经没用了，还留着容易误导后来人。

---

### 问题 6 ｜「任务计划」和「改动小结基线」是全进程共用的，不跟对话走

这两样东西存在 MCP 进程里（server.js 第 145-152 行：baselineSnapshot、currentTodos），**不分对话**。

**后果**：
1. 你在 A 对话里让 AI 列了 5 步计划，切到 B 对话（或点「新对话」）后，B 对话里 AI 更新计划会把 A 的计划覆盖掉；界面上那个计划卡片是全局唯一节点，也会跟着换内容。
2. 每次打开 AI 面板都会悄悄把「当前工程状态」设成基线（ai-agent.js 第 2812 行），意思是：**你打开面板之前的所有改动，都不会出现在这次的「改动小结」里**。如果用户是先让 AI 改了一轮、再关掉面板又打开，小结就会少报。

---

## 三、中等（会让人困惑或偶发出错，但不至于丢数据）

7. **write_resource 没有并发保护**。其它写工具（write_script / edit_script / patch_resource / append_event_commands / upsert_database_item / delete_resource）都会带「改动前的指纹」做冲突校验，只有 write_resource 的预览不返回这个指纹，于是**只有它可能盖掉别人刚改的内容**。
8. **系统提示词让模型「先预览再改」，但又要求它调用本来就是「执行」动作的工具**。提示词第 24 条列了 9 个写工具让模型先 dryRun，可其中 append_event_commands / upsert_database_item / create_script 等根本没声明 dryRun 参数，模型传了也是白传；提示词第 17 条还写着「优先用 ui_steps 演出来」，而 ui_steps 在别处又要求它先 dryRun。模型容易在这几处来回打转。
9. **edit_script 编译失败回滚时不记账**（server.js 第 1282-1286 行没有调用 rememberWrite），而 write_script 的同款路径有记。结果是「改动小结」里少一条「这次编译没过、已回滚」的证据。
10. **project_changelog 被标成「只读」**（readOnlyHint: true），于是它可能和别的只读工具**同时**执行——但它传 reset:true 时会改全局基线。并发跑就会把基线拍在错误的时间点上。
11. **UI 操作与试玩操作被放进了同一个「危险动作」名单**：在「自动模式」下，click_element、trigger_playtest、send_player_input、send_player_pointer、playtest_smoke 都会**不经确认直接执行**（文件修改仍然要确认，这点没问题）。用户以为确认过的只有自动化点界面，实际试玩里的一串按键也走了同一条路。
12. **删资源其实可以绕过「被引用保护」**：delete_resource 发现资源被别的文件引用时会拒绝，但只要传 force: true 就继续删（server.js 第 1408 行）。系统提示词没有禁止模型用它，而交接文档里的建议是「首版最好完全不提供 force」。另外这个引用检查只看 Assets 里的部分文件类型、且最多看 5 个文件就停。
13. **令牌退化的兜底路径不统一**：sharedToken() 读不到文件时会去看 localStorage，而 ensureHost() 用的是另一个 key。同一台机器上可能出现「文件读不到 → 每次调用生成新令牌」，症状就是面板一直连不上（同问题 5）。

---

## 四、次要（小毛病，攒着一起看）

14. **create_script 预览不带「改动前指纹」**，正式写入时也就没有并发校验（和问题 7 同源）。
15. **create_script 要求文件名的 GUID 必须「含 a-f」**，随机生成的 16 位十六进制里如果没有字母就会被拒；工具虽然提供了 generate_guid，但流程上没串起来，模型可能得试几次。
16. **宿主两套脏检查并存**：MCP 那边的「未保存修改」检查几乎无效（问题 1），宿主那边的「未失焦输入」检查有效，两者管的是同一件事。
17. **审批续跑时「引导」会走错路**：续跑期间会话没有标记忙碌，用户这时按 Ctrl+Enter 补充的话，宿主会回一句「现在没在跑，直接发就行」，实际上正跑着——只能落进排队区。
18. **审批续跑的用量行只统计「续跑那一段」**（runTurn 会重置本轮记账），所以「本轮 N 次调用 / 花了多少钱」偏小。
19. **切工程会清空内存里的会话**（/project 路由），历史文件还在、能找回，但当前对话会被打断。
20. **工具名和界面对不上**：界面上工具卡片叫「精确改脚本 / 在界面上演示操作」，工具列表里却是 edit_script / ui_steps（ai-host.js 的 TOOL_LABELS 与 server.js 的工具名两套写法）。
21. **「读取界面结构」等工具没列进宿主的中文名表**，卡片上会露出英文原名。
22. **撤销面板可能有点慢**：每次打开都会把备份目录里所有没有元数据的备份文件逐个读一遍算指纹（file-ops.js 第 147-150 行），备份多了会卡。
23. **单轮步数选了「无限制」时，兜底文案会显示「已完成单轮上限（Infinity 步）」**（真实情况是它只在模型真的打转时才会停）。
24. **小卫生问题**：会话删除后几个内部表格不清（内存慢慢长）；面板每轮收尾会把 /status 请求发两遍；每次打开面板都会重新拉一次可用模型列表。

---

## 五、确认没问题的部分（免得白操心）

- 回归套件：**30 套 30 通过**（我实跑了 node tests/run-all.cjs）。
- 工具结果超长：会裁剪、会**如实标注「已截断」**、会把完整原文落盘并给路径——而且明确告诉模型「那个路径你读不到」，这句话写得很实在。
- 编译门禁：写入后强制跑引擎自带的 tsc，不过就回滚；「找不到编译器」和「编译没过」被区分开（前者不回滚），这个区分很重要，做对了。
- 删除资源：走一次性确认令牌，令牌用完即废、不能重放；伪造令牌会被拒。
- 消息序列自愈：带「工具调用」却没有应答的坏历史，会在每次发请求前自动补齐；上游 400 拒绝序列时还会自愈重试一次。
- 打断：取消信号会一路传到模型请求和工具执行，Node 里「主动断开不触发 error 导致永久挂起」这个坑已经堵住了。
- 上下文压缩算法本身（结构化检查点、只折「组边界」不折一半工具调用）：逻辑是对的，问题只在**触发阈值和保留比例**（问题 2）。
- 试玩桥：控制接口要令牌，读令牌的接口拒绝带来源的浏览器请求。
- 路径安全：越界的相对路径（.. 出工程根）会被拒绝。

---

## 六、建议的修复顺序（本次不动手，仅供排期）

| 优先级 | 事项 | 对应问题 |
| :--- | :--- | :--- |
| P0 | 让「编辑器有未保存修改」真的能拦住写入（拦不住时要如实告诉用户「这次没查」） | 1 |
| P0 | 把压缩阈值与保留量改成同一套口径，界面显示改成说真话 | 2 |
| P1 | 审批续跑期间给会话上锁 | 4 |
| P1 | 令牌：读不到文件就明确报错，不要偷偷换一个新的 | 5、13 |
| P1 | 新建脚本/事件的确认卡补上「将要写入的内容」 | 3 |
| P2 | 计划与基线改成跟对话走；write_resource 补并发保护；会改状态的工具改回「非只读」 | 6、7、10 |
| P3 | 收尾清单（14-24） | — |
